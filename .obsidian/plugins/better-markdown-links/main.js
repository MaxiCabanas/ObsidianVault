/*
THIS IS A GENERATED/BUNDLED FILE BY ESBUILD
if you want to view the source, please visit the github repository of this plugin
*/

"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __commonJS = (cb, mod) => function __require() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// node_modules/eventemitter3/index.js
var require_eventemitter3 = __commonJS({
  "node_modules/eventemitter3/index.js"(exports2, module2) {
    var __import_meta_url9 = globalThis["import.meta.url"] ?? (() => {
      if (typeof __filename !== "string") {
        return new URL(window.location.href);
      }
      return require("node:url").pathToFileURL(__filename);
    })();
    var __process9 = globalThis["process"] ?? {
      "cwd": () => "/",
      "env": {},
      "platform": "android"
    };
    var has = Object.prototype.hasOwnProperty;
    var prefix = "~";
    function Events() {
    }
    if (Object.create) {
      Events.prototype = /* @__PURE__ */ Object.create(null);
      if (!new Events().__proto__) prefix = false;
    }
    function EE(fn, context, once) {
      this.fn = fn;
      this.context = context;
      this.once = once || false;
    }
    function addListener(emitter, event, fn, context, once) {
      if (typeof fn !== "function") {
        throw new TypeError("The listener must be a function");
      }
      var listener = new EE(fn, context || emitter, once), evt = prefix ? prefix + event : event;
      if (!emitter._events[evt]) emitter._events[evt] = listener, emitter._eventsCount++;
      else if (!emitter._events[evt].fn) emitter._events[evt].push(listener);
      else emitter._events[evt] = [emitter._events[evt], listener];
      return emitter;
    }
    function clearEvent(emitter, evt) {
      if (--emitter._eventsCount === 0) emitter._events = new Events();
      else delete emitter._events[evt];
    }
    function EventEmitter() {
      this._events = new Events();
      this._eventsCount = 0;
    }
    EventEmitter.prototype.eventNames = function eventNames() {
      var names = [], events, name;
      if (this._eventsCount === 0) return names;
      for (name in events = this._events) {
        if (has.call(events, name)) names.push(prefix ? name.slice(1) : name);
      }
      if (Object.getOwnPropertySymbols) {
        return names.concat(Object.getOwnPropertySymbols(events));
      }
      return names;
    };
    EventEmitter.prototype.listeners = function listeners(event) {
      var evt = prefix ? prefix + event : event, handlers = this._events[evt];
      if (!handlers) return [];
      if (handlers.fn) return [handlers.fn];
      for (var i = 0, l = handlers.length, ee = new Array(l); i < l; i++) {
        ee[i] = handlers[i].fn;
      }
      return ee;
    };
    EventEmitter.prototype.listenerCount = function listenerCount(event) {
      var evt = prefix ? prefix + event : event, listeners = this._events[evt];
      if (!listeners) return 0;
      if (listeners.fn) return 1;
      return listeners.length;
    };
    EventEmitter.prototype.emit = function emit(event, a1, a2, a3, a4, a5) {
      var evt = prefix ? prefix + event : event;
      if (!this._events[evt]) return false;
      var listeners = this._events[evt], len = arguments.length, args, i;
      if (listeners.fn) {
        if (listeners.once) this.removeListener(event, listeners.fn, void 0, true);
        switch (len) {
          case 1:
            return listeners.fn.call(listeners.context), true;
          case 2:
            return listeners.fn.call(listeners.context, a1), true;
          case 3:
            return listeners.fn.call(listeners.context, a1, a2), true;
          case 4:
            return listeners.fn.call(listeners.context, a1, a2, a3), true;
          case 5:
            return listeners.fn.call(listeners.context, a1, a2, a3, a4), true;
          case 6:
            return listeners.fn.call(listeners.context, a1, a2, a3, a4, a5), true;
        }
        for (i = 1, args = new Array(len - 1); i < len; i++) {
          args[i - 1] = arguments[i];
        }
        listeners.fn.apply(listeners.context, args);
      } else {
        var length = listeners.length, j;
        for (i = 0; i < length; i++) {
          if (listeners[i].once) this.removeListener(event, listeners[i].fn, void 0, true);
          switch (len) {
            case 1:
              listeners[i].fn.call(listeners[i].context);
              break;
            case 2:
              listeners[i].fn.call(listeners[i].context, a1);
              break;
            case 3:
              listeners[i].fn.call(listeners[i].context, a1, a2);
              break;
            case 4:
              listeners[i].fn.call(listeners[i].context, a1, a2, a3);
              break;
            default:
              if (!args) for (j = 1, args = new Array(len - 1); j < len; j++) {
                args[j - 1] = arguments[j];
              }
              listeners[i].fn.apply(listeners[i].context, args);
          }
        }
      }
      return true;
    };
    EventEmitter.prototype.on = function on(event, fn, context) {
      return addListener(this, event, fn, context, false);
    };
    EventEmitter.prototype.once = function once(event, fn, context) {
      return addListener(this, event, fn, context, true);
    };
    EventEmitter.prototype.removeListener = function removeListener(event, fn, context, once) {
      var evt = prefix ? prefix + event : event;
      if (!this._events[evt]) return this;
      if (!fn) {
        clearEvent(this, evt);
        return this;
      }
      var listeners = this._events[evt];
      if (listeners.fn) {
        if (listeners.fn === fn && (!once || listeners.once) && (!context || listeners.context === context)) {
          clearEvent(this, evt);
        }
      } else {
        for (var i = 0, events = [], length = listeners.length; i < length; i++) {
          if (listeners[i].fn !== fn || once && !listeners[i].once || context && listeners[i].context !== context) {
            events.push(listeners[i]);
          }
        }
        if (events.length) this._events[evt] = events.length === 1 ? events[0] : events;
        else clearEvent(this, evt);
      }
      return this;
    };
    EventEmitter.prototype.removeAllListeners = function removeAllListeners(event) {
      var evt;
      if (event) {
        evt = prefix ? prefix + event : event;
        if (this._events[evt]) clearEvent(this, evt);
      } else {
        this._events = new Events();
        this._eventsCount = 0;
      }
      return this;
    };
    EventEmitter.prototype.off = EventEmitter.prototype.removeListener;
    EventEmitter.prototype.addListener = EventEmitter.prototype.on;
    EventEmitter.prefixed = prefix;
    EventEmitter.EventEmitter = EventEmitter;
    if ("undefined" !== typeof module2) {
      module2.exports = EventEmitter;
    }
  }
});

// node_modules/obsidian-dev-utils/dist/lib/Error.cjs
var require_Error = __commonJS({
  "node_modules/obsidian-dev-utils/dist/lib/Error.cjs"(exports2, module2) {
    (function patchRequireEsmDefault() {
      const __require = require;
      require = Object.assign((id) => {
        const module3 = __require(id);
        return module3.__esModule && module3.default ? module3.default : module3;
      }, __require);
    })();
    var __defProp2 = Object.defineProperty;
    var __getOwnPropDesc2 = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames2 = Object.getOwnPropertyNames;
    var __hasOwnProp2 = Object.prototype.hasOwnProperty;
    var __export2 = (target, all) => {
      for (var name in all)
        __defProp2(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps2 = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames2(from))
          if (!__hasOwnProp2.call(to, key) && key !== except)
            __defProp2(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc2(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps2(__defProp2({}, "__esModule", { value: true }), mod);
    var Error_exports = {};
    __export2(Error_exports, {
      emitAsyncErrorEvent: () => emitAsyncErrorEvent2,
      errorToString: () => errorToString,
      getStackTrace: () => getStackTrace,
      printError: () => printError,
      registerAsyncErrorEventHandler: () => registerAsyncErrorEventHandler,
      throwExpression: () => throwExpression
    });
    module2.exports = __toCommonJS2(Error_exports);
    var import_eventemitter3 = require_eventemitter3();
    var __import_meta_url9 = globalThis["import.meta.url"] ?? (() => {
      if (typeof __filename !== "string") {
        return new URL(window.location.href);
      }
      return require("node:url").pathToFileURL(__filename);
    })();
    var __process9 = globalThis["process"] ?? {
      "cwd": () => "/",
      "env": {},
      "platform": "android"
    };
    var ASYNC_ERROR_EVENT = "asyncError";
    var asyncErrorEventEmitter = new import_eventemitter3.EventEmitter();
    asyncErrorEventEmitter.on(ASYNC_ERROR_EVENT, handleAsyncError);
    function handleAsyncError(asyncError) {
      printError(new Error("An unhandled error occurred executing async operation", { cause: asyncError }));
    }
    function emitAsyncErrorEvent2(asyncError) {
      asyncErrorEventEmitter.emit(ASYNC_ERROR_EVENT, asyncError);
    }
    function registerAsyncErrorEventHandler(handler) {
      asyncErrorEventEmitter.on(ASYNC_ERROR_EVENT, handler);
      return () => asyncErrorEventEmitter.off(ASYNC_ERROR_EVENT, handler);
    }
    function printError(error) {
      const entries = parseErrorEntries(error);
      for (const entry of entries) {
        if (entry.shouldClearAnsiSequence) {
          console.error(`\x1B[0m${entry.message}\x1B[0m`);
        } else {
          console.error(entry.message);
        }
      }
    }
    function errorToString(error) {
      return parseErrorEntries(error).map((entry) => "  ".repeat(entry.level) + entry.message).join("\n");
    }
    function parseErrorEntries(error, level = 0, entries = []) {
      if (error === void 0) {
        return entries;
      }
      if (!(error instanceof Error)) {
        let str = "";
        if (error === null) {
          str = "(null)";
        } else if (typeof error === "string") {
          str = error;
        } else {
          str = JSON.stringify(error);
        }
        entries.push({ level, message: str });
        return entries;
      }
      const title = `${error.name}: ${error.message}`;
      entries.push({ level, message: title, shouldClearAnsiSequence: true });
      if (error.stack) {
        const restStack = error.stack.startsWith(title) ? error.stack.substring(title.length + 1) : error.stack;
        entries.push({ level, message: `Error stack:
${restStack}` });
      }
      if (error.cause !== void 0) {
        entries.push({ level, message: "Caused by:" });
        parseErrorEntries(error.cause, level + 1, entries);
      }
      return entries;
    }
    function throwExpression(error) {
      throw error;
    }
    function getStackTrace() {
      const stack = new Error().stack ?? "";
      const lines = stack.split("\n");
      return lines.slice(2).join("\n");
    }
  }
});

// node_modules/obsidian-dev-utils/dist/lib/Async.cjs
var require_Async = __commonJS({
  "node_modules/obsidian-dev-utils/dist/lib/Async.cjs"(exports2, module2) {
    (function patchRequireEsmDefault() {
      const __require = require;
      require = Object.assign((id) => {
        const module3 = __require(id);
        return module3.__esModule && module3.default ? module3.default : module3;
      }, __require);
    })();
    var __defProp2 = Object.defineProperty;
    var __getOwnPropDesc2 = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames2 = Object.getOwnPropertyNames;
    var __hasOwnProp2 = Object.prototype.hasOwnProperty;
    var __export2 = (target, all) => {
      for (var name in all)
        __defProp2(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps2 = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames2(from))
          if (!__hasOwnProp2.call(to, key) && key !== except)
            __defProp2(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc2(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps2(__defProp2({}, "__esModule", { value: true }), mod);
    var Async_exports = {};
    __export2(Async_exports, {
      addErrorHandler: () => addErrorHandler,
      asyncFilter: () => asyncFilter,
      asyncFlatMap: () => asyncFlatMap,
      asyncMap: () => asyncMap,
      convertAsyncToSync: () => convertAsyncToSync,
      convertSyncToAsync: () => convertSyncToAsync,
      invokeAsyncSafely: () => invokeAsyncSafely,
      retryWithTimeout: () => retryWithTimeout,
      runWithTimeout: () => runWithTimeout,
      sleep: () => sleep,
      timeout: () => timeout,
      toArray: () => toArray
    });
    module2.exports = __toCommonJS2(Async_exports);
    var import_Error2 = require_Error();
    var __import_meta_url9 = globalThis["import.meta.url"] ?? (() => {
      if (typeof __filename !== "string") {
        return new URL(window.location.href);
      }
      return require("node:url").pathToFileURL(__filename);
    })();
    var __process9 = globalThis["process"] ?? {
      "cwd": () => "/",
      "env": {},
      "platform": "android"
    };
    async function retryWithTimeout(asyncFn, retryOptions = {}) {
      const stackTrace = (0, import_Error2.getStackTrace)();
      const DEFAULT_RETRY_OPTIONS = {
        timeoutInMilliseconds: 5e3,
        retryDelayInMilliseconds: 100
      };
      const overriddenOptions = { ...DEFAULT_RETRY_OPTIONS, ...retryOptions };
      await runWithTimeout(overriddenOptions.timeoutInMilliseconds, async () => {
        let attempt = 0;
        for (; ; ) {
          attempt++;
          if (await asyncFn()) {
            if (attempt > 1) {
              console.debug(`Retry completed successfully after ${attempt.toString()} attempts`);
            }
            return;
          }
          console.debug(`Retry attempt ${attempt.toString()} completed unsuccessfully. Trying again in ${overriddenOptions.retryDelayInMilliseconds.toString()} milliseconds`, {
            asyncFn,
            stackTrace
          });
          await sleep(overriddenOptions.retryDelayInMilliseconds);
        }
      });
    }
    async function sleep(milliseconds) {
      await new Promise((resolve) => setTimeout(resolve, milliseconds));
    }
    async function runWithTimeout(timeoutInMilliseconds, asyncFn) {
      return await Promise.race([asyncFn(), timeout(timeoutInMilliseconds)]);
    }
    async function timeout(timeoutInMilliseconds) {
      await sleep(timeoutInMilliseconds);
      throw new Error(`Timed out in ${timeoutInMilliseconds.toString()} milliseconds`);
    }
    function invokeAsyncSafely(asyncFn) {
      void addErrorHandler(asyncFn);
    }
    async function addErrorHandler(asyncFn) {
      try {
        await asyncFn();
      } catch (asyncError) {
        (0, import_Error2.emitAsyncErrorEvent)(asyncError);
      }
    }
    function convertAsyncToSync(asyncFunc) {
      return (...args) => {
        invokeAsyncSafely(() => asyncFunc(...args));
      };
    }
    function convertSyncToAsync(syncFn) {
      return (...args) => Promise.resolve().then(() => syncFn(...args));
    }
    async function asyncMap(arr, callback) {
      return await Promise.all(arr.map(callback));
    }
    async function asyncFilter(arr, predicate) {
      const predicateResults = await asyncMap(arr, predicate);
      return arr.filter((_, index) => predicateResults[index]);
    }
    async function asyncFlatMap(arr, callback) {
      return (await asyncMap(arr, callback)).flat();
    }
    async function toArray(iter) {
      const arr = [];
      for await (const item of iter) {
        arr.push(item);
      }
      return arr;
    }
  }
});

// node_modules/obsidian-dev-utils/dist/lib/obsidian/App.cjs
var require_App = __commonJS({
  "node_modules/obsidian-dev-utils/dist/lib/obsidian/App.cjs"(exports2, module2) {
    (function patchRequireEsmDefault() {
      const __require = require;
      require = Object.assign((id) => {
        const module3 = __require(id);
        return module3.__esModule && module3.default ? module3.default : module3;
      }, __require);
    })();
    var __defProp2 = Object.defineProperty;
    var __getOwnPropDesc2 = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames2 = Object.getOwnPropertyNames;
    var __hasOwnProp2 = Object.prototype.hasOwnProperty;
    var __export2 = (target, all) => {
      for (var name in all)
        __defProp2(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps2 = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames2(from))
          if (!__hasOwnProp2.call(to, key) && key !== except)
            __defProp2(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc2(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps2(__defProp2({}, "__esModule", { value: true }), mod);
    var App_exports = {};
    __export2(App_exports, {
      ValueWrapper: () => ValueWrapper,
      getApp: () => getApp,
      getObsidianDevUtilsState: () => getObsidianDevUtilsState
    });
    module2.exports = __toCommonJS2(App_exports);
    var __import_meta_url9 = globalThis["import.meta.url"] ?? (() => {
      if (typeof __filename !== "string") {
        return new URL(window.location.href);
      }
      return require("node:url").pathToFileURL(__filename);
    })();
    var __process9 = globalThis["process"] ?? {
      "cwd": () => "/",
      "env": {},
      "platform": "android"
    };
    function getApp() {
      let canRequire;
      try {
        globalThis.require.resolve("obsidian/app");
        canRequire = true;
      } catch {
        canRequire = false;
      }
      if (canRequire) {
        return globalThis.require("obsidian/app");
      }
      const app = globalThis.app;
      if (app) {
        return app;
      }
      throw new Error("Obsidian app not found");
    }
    var ValueWrapper = class {
      constructor(value) {
        this.value = value;
      }
    };
    function getObsidianDevUtilsState(app, key, defaultValue) {
      const sharedStateWrapper = app;
      const sharedState = sharedStateWrapper.obsidianDevUtilsState ??= {};
      return sharedState[key] ??= new ValueWrapper(defaultValue);
    }
  }
});

// node_modules/obsidian-dev-utils/dist/lib/obsidian/Logger.cjs
var require_Logger = __commonJS({
  "node_modules/obsidian-dev-utils/dist/lib/obsidian/Logger.cjs"(exports2, module2) {
    (function patchRequireEsmDefault() {
      const __require = require;
      require = Object.assign((id) => {
        const module3 = __require(id);
        return module3.__esModule && module3.default ? module3.default : module3;
      }, __require);
    })();
    var __defProp2 = Object.defineProperty;
    var __getOwnPropDesc2 = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames2 = Object.getOwnPropertyNames;
    var __hasOwnProp2 = Object.prototype.hasOwnProperty;
    var __export2 = (target, all) => {
      for (var name in all)
        __defProp2(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps2 = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames2(from))
          if (!__hasOwnProp2.call(to, key) && key !== except)
            __defProp2(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc2(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps2(__defProp2({}, "__esModule", { value: true }), mod);
    var Logger_exports = {};
    __export2(Logger_exports, {
      invokeAndLog: () => invokeAndLog,
      invokeAsyncAndLog: () => invokeAsyncAndLog
    });
    module2.exports = __toCommonJS2(Logger_exports);
    var import_Error2 = require_Error();
    var __import_meta_url9 = globalThis["import.meta.url"] ?? (() => {
      if (typeof __filename !== "string") {
        return new URL(window.location.href);
      }
      return require("node:url").pathToFileURL(__filename);
    })();
    var __process9 = globalThis["process"] ?? {
      "cwd": () => "/",
      "env": {},
      "platform": "android"
    };
    function invokeAndLog(title, fn, stackTrace) {
      void invokeAsyncAndLog(title, async () => {
        fn();
        await Promise.resolve();
      }, stackTrace);
    }
    async function invokeAsyncAndLog(title, fn, stackTrace) {
      const timestampStart = Date.now();
      if (stackTrace === void 0) {
        stackTrace = (0, import_Error2.getStackTrace)().split("\n").slice(1).join("\n");
      }
      console.debug(`${title}:start`, {
        timestampStart,
        fn,
        stackTrace
      });
      try {
        await fn();
        const timestampEnd = Date.now();
        console.debug(`${title}:end`, {
          timestampStart,
          timestampEnd,
          duration: timestampEnd - timestampStart
        });
      } catch (error) {
        const timestampEnd = Date.now();
        console.debug(`${title}:error`, {
          timestampStart,
          timestampEnd: Date.now(),
          duration: timestampEnd - timestampStart,
          error
        });
        throw error;
      }
    }
  }
});

// node_modules/obsidian-dev-utils/dist/lib/obsidian/ChainedPromise.cjs
var require_ChainedPromise = __commonJS({
  "node_modules/obsidian-dev-utils/dist/lib/obsidian/ChainedPromise.cjs"(exports2, module2) {
    (function patchRequireEsmDefault() {
      const __require = require;
      require = Object.assign((id) => {
        const module3 = __require(id);
        return module3.__esModule && module3.default ? module3.default : module3;
      }, __require);
    })();
    var __defProp2 = Object.defineProperty;
    var __getOwnPropDesc2 = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames2 = Object.getOwnPropertyNames;
    var __hasOwnProp2 = Object.prototype.hasOwnProperty;
    var __export2 = (target, all) => {
      for (var name in all)
        __defProp2(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps2 = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames2(from))
          if (!__hasOwnProp2.call(to, key) && key !== except)
            __defProp2(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc2(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps2(__defProp2({}, "__esModule", { value: true }), mod);
    var ChainedPromise_exports = {};
    __export2(ChainedPromise_exports, {
      chainAsyncFn: () => chainAsyncFn3
    });
    module2.exports = __toCommonJS2(ChainedPromise_exports);
    var import_Async = require_Async();
    var import_Error2 = require_Error();
    var import_App = require_App();
    var import_Logger = require_Logger();
    var __import_meta_url9 = globalThis["import.meta.url"] ?? (() => {
      if (typeof __filename !== "string") {
        return new URL(window.location.href);
      }
      return require("node:url").pathToFileURL(__filename);
    })();
    var __process9 = globalThis["process"] ?? {
      "cwd": () => "/",
      "env": {},
      "platform": "android"
    };
    function getChainedPromiseWrapper(app) {
      return (0, import_App.getObsidianDevUtilsState)(app, "chainedPromise", Promise.resolve());
    }
    function chainAsyncFn3(app, asyncFn) {
      const stackTrace = (0, import_Error2.getStackTrace)();
      const chainedPromiseWrapper = getChainedPromiseWrapper(app);
      chainedPromiseWrapper.value = chainedPromiseWrapper.value.then(() => (0, import_Async.addErrorHandler)(() => (0, import_Logger.invokeAsyncAndLog)("chainAsyncFn", asyncFn, stackTrace)));
    }
  }
});

// node_modules/obsidian-typings/dist/implementations.cjs
var require_implementations = __commonJS({
  "node_modules/obsidian-typings/dist/implementations.cjs"(exports2, module2) {
    var __import_meta_url9 = globalThis["import.meta.url"] ?? (() => {
      if (typeof __filename !== "string") {
        return new URL(window.location.href);
      }
      return require("node:url").pathToFileURL(__filename);
    })();
    var __process9 = globalThis["process"] ?? {
      "cwd": () => "/",
      "env": {},
      "platform": "android"
    };
    var __defProp2 = Object.defineProperty;
    var __getOwnPropDesc2 = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames2 = Object.getOwnPropertyNames;
    var __hasOwnProp2 = Object.prototype.hasOwnProperty;
    var __export2 = (target, all) => {
      for (var name in all)
        __defProp2(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps2 = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames2(from))
          if (!__hasOwnProp2.call(to, key) && key !== except)
            __defProp2(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc2(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps2(__defProp2({}, "__esModule", { value: true }), mod);
    var implementations_exports = {};
    __export2(implementations_exports, {
      CustomArrayDictImpl: () => CustomArrayDictImpl,
      InternalPluginName: () => InternalPluginName,
      createTFileInstance: () => createTFileInstance,
      createTFolderInstance: () => createTFolderInstance,
      parentFolderPath: () => parentFolderPath
    });
    module2.exports = __toCommonJS2(implementations_exports);
    var CustomArrayDictImpl = class {
      data = {};
      add(key, value) {
        if (!this.data.hasOwnProperty(key)) {
          this.data[key] = [];
        }
        const values = this.data[key];
        if (!values.includes(value)) {
          values.push(value);
        }
      }
      remove(key, value) {
        const values = this.data[key];
        if (!values) {
          return;
        }
        values.remove(value);
        if (values.length === 0) {
          delete this.data[key];
        }
      }
      removeKey(key) {
        delete this.data[key];
      }
      get(key) {
        return this.data.hasOwnProperty(key) ? this.data[key] : null;
      }
      keys() {
        return Object.keys(this.data);
      }
      clear(key) {
        delete this.data[key];
      }
      clearAll() {
        this.data = {};
      }
      contains(key, value) {
        const values = this.data[key];
        return values && values.contains(value) || false;
      }
      count() {
        let ans = 0;
        for (const key in this.data) {
          if (this.data.hasOwnProperty(key)) {
            ans += this.data[key].length;
          }
        }
        return ans;
      }
    };
    var import_obsidian5 = require("obsidian");
    function parentFolderPath(path) {
      return path.replace(/\/?[^\/]*$/, "") || "/";
    }
    function createTFolderInstance(vault, path) {
      let folder = vault.getFolderByPath(path);
      if (folder) {
        return folder;
      }
      folder = new import_obsidian5.TFolder(vault, path);
      folder.parent = createTFolderInstance(vault, parentFolderPath(path));
      folder.deleted = true;
      return folder;
    }
    var import_obsidian22 = require("obsidian");
    function createTFileInstance(vault, path) {
      let file = vault.getFileByPath(path);
      if (file) {
        return file;
      }
      file = new import_obsidian22.TFile(vault, path);
      file.parent = createTFolderInstance(vault, parentFolderPath(path));
      file.deleted = true;
      return file;
    }
    var InternalPluginName = {
      AudioRecorder: "audio-recorder",
      Backlink: "backlink",
      Bookmarks: "bookmarks",
      Canvas: "canvas",
      CommandPalette: "command-palette",
      DailyNotes: "daily-notes",
      EditorStatus: "editor-status",
      FileExplorer: "file-explorer",
      FileRecovery: "file-recovery",
      GlobalSearch: "global-search",
      Graph: "graph",
      MarkdownImporter: "markdown-importer",
      NoteComposer: "note-composer",
      OutgoingLink: "outgoing-link",
      Outline: "outline",
      PagePreview: "page-preview",
      Properties: "properties",
      Publish: "publish",
      RandomNote: "random-note",
      SlashCommand: "slash-command",
      Slides: "slides",
      Starred: "starred",
      Switcher: "switcher",
      Sync: "sync",
      TagPane: "tag-pane",
      Templates: "templates",
      WordCount: "word-count",
      Workspaces: "workspaces",
      ZkPrefixer: "zk-prefixer"
    };
  }
});

// node_modules/path-browserify/index.js
var require_path_browserify = __commonJS({
  "node_modules/path-browserify/index.js"(exports2, module2) {
    var __import_meta_url9 = globalThis["import.meta.url"] ?? (() => {
      if (typeof __filename !== "string") {
        return new URL(window.location.href);
      }
      return require("node:url").pathToFileURL(__filename);
    })();
    var __process9 = globalThis["process"] ?? {
      "cwd": () => "/",
      "env": {},
      "platform": "android"
    };
    function assertPath(path) {
      if (typeof path !== "string") {
        throw new TypeError("Path must be a string. Received " + JSON.stringify(path));
      }
    }
    function normalizeStringPosix(path, allowAboveRoot) {
      var res = "";
      var lastSegmentLength = 0;
      var lastSlash = -1;
      var dots = 0;
      var code;
      for (var i = 0; i <= path.length; ++i) {
        if (i < path.length)
          code = path.charCodeAt(i);
        else if (code === 47)
          break;
        else
          code = 47;
        if (code === 47) {
          if (lastSlash === i - 1 || dots === 1) {
          } else if (lastSlash !== i - 1 && dots === 2) {
            if (res.length < 2 || lastSegmentLength !== 2 || res.charCodeAt(res.length - 1) !== 46 || res.charCodeAt(res.length - 2) !== 46) {
              if (res.length > 2) {
                var lastSlashIndex = res.lastIndexOf("/");
                if (lastSlashIndex !== res.length - 1) {
                  if (lastSlashIndex === -1) {
                    res = "";
                    lastSegmentLength = 0;
                  } else {
                    res = res.slice(0, lastSlashIndex);
                    lastSegmentLength = res.length - 1 - res.lastIndexOf("/");
                  }
                  lastSlash = i;
                  dots = 0;
                  continue;
                }
              } else if (res.length === 2 || res.length === 1) {
                res = "";
                lastSegmentLength = 0;
                lastSlash = i;
                dots = 0;
                continue;
              }
            }
            if (allowAboveRoot) {
              if (res.length > 0)
                res += "/..";
              else
                res = "..";
              lastSegmentLength = 2;
            }
          } else {
            if (res.length > 0)
              res += "/" + path.slice(lastSlash + 1, i);
            else
              res = path.slice(lastSlash + 1, i);
            lastSegmentLength = i - lastSlash - 1;
          }
          lastSlash = i;
          dots = 0;
        } else if (code === 46 && dots !== -1) {
          ++dots;
        } else {
          dots = -1;
        }
      }
      return res;
    }
    function _format(sep, pathObject) {
      var dir = pathObject.dir || pathObject.root;
      var base = pathObject.base || (pathObject.name || "") + (pathObject.ext || "");
      if (!dir) {
        return base;
      }
      if (dir === pathObject.root) {
        return dir + base;
      }
      return dir + sep + base;
    }
    var posix = {
      // path.resolve([from ...], to)
      resolve: function resolve() {
        var resolvedPath = "";
        var resolvedAbsolute = false;
        var cwd;
        for (var i = arguments.length - 1; i >= -1 && !resolvedAbsolute; i--) {
          var path;
          if (i >= 0)
            path = arguments[i];
          else {
            if (cwd === void 0)
              cwd = __process9.cwd();
            path = cwd;
          }
          assertPath(path);
          if (path.length === 0) {
            continue;
          }
          resolvedPath = path + "/" + resolvedPath;
          resolvedAbsolute = path.charCodeAt(0) === 47;
        }
        resolvedPath = normalizeStringPosix(resolvedPath, !resolvedAbsolute);
        if (resolvedAbsolute) {
          if (resolvedPath.length > 0)
            return "/" + resolvedPath;
          else
            return "/";
        } else if (resolvedPath.length > 0) {
          return resolvedPath;
        } else {
          return ".";
        }
      },
      normalize: function normalize(path) {
        assertPath(path);
        if (path.length === 0) return ".";
        var isAbsolute = path.charCodeAt(0) === 47;
        var trailingSeparator = path.charCodeAt(path.length - 1) === 47;
        path = normalizeStringPosix(path, !isAbsolute);
        if (path.length === 0 && !isAbsolute) path = ".";
        if (path.length > 0 && trailingSeparator) path += "/";
        if (isAbsolute) return "/" + path;
        return path;
      },
      isAbsolute: function isAbsolute(path) {
        assertPath(path);
        return path.length > 0 && path.charCodeAt(0) === 47;
      },
      join: function join() {
        if (arguments.length === 0)
          return ".";
        var joined;
        for (var i = 0; i < arguments.length; ++i) {
          var arg = arguments[i];
          assertPath(arg);
          if (arg.length > 0) {
            if (joined === void 0)
              joined = arg;
            else
              joined += "/" + arg;
          }
        }
        if (joined === void 0)
          return ".";
        return posix.normalize(joined);
      },
      relative: function relative(from, to) {
        assertPath(from);
        assertPath(to);
        if (from === to) return "";
        from = posix.resolve(from);
        to = posix.resolve(to);
        if (from === to) return "";
        var fromStart = 1;
        for (; fromStart < from.length; ++fromStart) {
          if (from.charCodeAt(fromStart) !== 47)
            break;
        }
        var fromEnd = from.length;
        var fromLen = fromEnd - fromStart;
        var toStart = 1;
        for (; toStart < to.length; ++toStart) {
          if (to.charCodeAt(toStart) !== 47)
            break;
        }
        var toEnd = to.length;
        var toLen = toEnd - toStart;
        var length = fromLen < toLen ? fromLen : toLen;
        var lastCommonSep = -1;
        var i = 0;
        for (; i <= length; ++i) {
          if (i === length) {
            if (toLen > length) {
              if (to.charCodeAt(toStart + i) === 47) {
                return to.slice(toStart + i + 1);
              } else if (i === 0) {
                return to.slice(toStart + i);
              }
            } else if (fromLen > length) {
              if (from.charCodeAt(fromStart + i) === 47) {
                lastCommonSep = i;
              } else if (i === 0) {
                lastCommonSep = 0;
              }
            }
            break;
          }
          var fromCode = from.charCodeAt(fromStart + i);
          var toCode = to.charCodeAt(toStart + i);
          if (fromCode !== toCode)
            break;
          else if (fromCode === 47)
            lastCommonSep = i;
        }
        var out = "";
        for (i = fromStart + lastCommonSep + 1; i <= fromEnd; ++i) {
          if (i === fromEnd || from.charCodeAt(i) === 47) {
            if (out.length === 0)
              out += "..";
            else
              out += "/..";
          }
        }
        if (out.length > 0)
          return out + to.slice(toStart + lastCommonSep);
        else {
          toStart += lastCommonSep;
          if (to.charCodeAt(toStart) === 47)
            ++toStart;
          return to.slice(toStart);
        }
      },
      _makeLong: function _makeLong(path) {
        return path;
      },
      dirname: function dirname(path) {
        assertPath(path);
        if (path.length === 0) return ".";
        var code = path.charCodeAt(0);
        var hasRoot = code === 47;
        var end = -1;
        var matchedSlash = true;
        for (var i = path.length - 1; i >= 1; --i) {
          code = path.charCodeAt(i);
          if (code === 47) {
            if (!matchedSlash) {
              end = i;
              break;
            }
          } else {
            matchedSlash = false;
          }
        }
        if (end === -1) return hasRoot ? "/" : ".";
        if (hasRoot && end === 1) return "//";
        return path.slice(0, end);
      },
      basename: function basename(path, ext) {
        if (ext !== void 0 && typeof ext !== "string") throw new TypeError('"ext" argument must be a string');
        assertPath(path);
        var start = 0;
        var end = -1;
        var matchedSlash = true;
        var i;
        if (ext !== void 0 && ext.length > 0 && ext.length <= path.length) {
          if (ext.length === path.length && ext === path) return "";
          var extIdx = ext.length - 1;
          var firstNonSlashEnd = -1;
          for (i = path.length - 1; i >= 0; --i) {
            var code = path.charCodeAt(i);
            if (code === 47) {
              if (!matchedSlash) {
                start = i + 1;
                break;
              }
            } else {
              if (firstNonSlashEnd === -1) {
                matchedSlash = false;
                firstNonSlashEnd = i + 1;
              }
              if (extIdx >= 0) {
                if (code === ext.charCodeAt(extIdx)) {
                  if (--extIdx === -1) {
                    end = i;
                  }
                } else {
                  extIdx = -1;
                  end = firstNonSlashEnd;
                }
              }
            }
          }
          if (start === end) end = firstNonSlashEnd;
          else if (end === -1) end = path.length;
          return path.slice(start, end);
        } else {
          for (i = path.length - 1; i >= 0; --i) {
            if (path.charCodeAt(i) === 47) {
              if (!matchedSlash) {
                start = i + 1;
                break;
              }
            } else if (end === -1) {
              matchedSlash = false;
              end = i + 1;
            }
          }
          if (end === -1) return "";
          return path.slice(start, end);
        }
      },
      extname: function extname(path) {
        assertPath(path);
        var startDot = -1;
        var startPart = 0;
        var end = -1;
        var matchedSlash = true;
        var preDotState = 0;
        for (var i = path.length - 1; i >= 0; --i) {
          var code = path.charCodeAt(i);
          if (code === 47) {
            if (!matchedSlash) {
              startPart = i + 1;
              break;
            }
            continue;
          }
          if (end === -1) {
            matchedSlash = false;
            end = i + 1;
          }
          if (code === 46) {
            if (startDot === -1)
              startDot = i;
            else if (preDotState !== 1)
              preDotState = 1;
          } else if (startDot !== -1) {
            preDotState = -1;
          }
        }
        if (startDot === -1 || end === -1 || // We saw a non-dot character immediately before the dot
        preDotState === 0 || // The (right-most) trimmed path component is exactly '..'
        preDotState === 1 && startDot === end - 1 && startDot === startPart + 1) {
          return "";
        }
        return path.slice(startDot, end);
      },
      format: function format(pathObject) {
        if (pathObject === null || typeof pathObject !== "object") {
          throw new TypeError('The "pathObject" argument must be of type Object. Received type ' + typeof pathObject);
        }
        return _format("/", pathObject);
      },
      parse: function parse(path) {
        assertPath(path);
        var ret = { root: "", dir: "", base: "", ext: "", name: "" };
        if (path.length === 0) return ret;
        var code = path.charCodeAt(0);
        var isAbsolute = code === 47;
        var start;
        if (isAbsolute) {
          ret.root = "/";
          start = 1;
        } else {
          start = 0;
        }
        var startDot = -1;
        var startPart = 0;
        var end = -1;
        var matchedSlash = true;
        var i = path.length - 1;
        var preDotState = 0;
        for (; i >= start; --i) {
          code = path.charCodeAt(i);
          if (code === 47) {
            if (!matchedSlash) {
              startPart = i + 1;
              break;
            }
            continue;
          }
          if (end === -1) {
            matchedSlash = false;
            end = i + 1;
          }
          if (code === 46) {
            if (startDot === -1) startDot = i;
            else if (preDotState !== 1) preDotState = 1;
          } else if (startDot !== -1) {
            preDotState = -1;
          }
        }
        if (startDot === -1 || end === -1 || // We saw a non-dot character immediately before the dot
        preDotState === 0 || // The (right-most) trimmed path component is exactly '..'
        preDotState === 1 && startDot === end - 1 && startDot === startPart + 1) {
          if (end !== -1) {
            if (startPart === 0 && isAbsolute) ret.base = ret.name = path.slice(1, end);
            else ret.base = ret.name = path.slice(startPart, end);
          }
        } else {
          if (startPart === 0 && isAbsolute) {
            ret.name = path.slice(1, startDot);
            ret.base = path.slice(1, end);
          } else {
            ret.name = path.slice(startPart, startDot);
            ret.base = path.slice(startPart, end);
          }
          ret.ext = path.slice(startDot, end);
        }
        if (startPart > 0) ret.dir = path.slice(0, startPart - 1);
        else if (isAbsolute) ret.dir = "/";
        return ret;
      },
      sep: "/",
      delimiter: ":",
      win32: null,
      posix: null
    };
    posix.posix = posix;
    module2.exports = posix;
  }
});

// node_modules/obsidian-dev-utils/dist/lib/RegExp.cjs
var require_RegExp = __commonJS({
  "node_modules/obsidian-dev-utils/dist/lib/RegExp.cjs"(exports2, module2) {
    (function patchRequireEsmDefault() {
      const __require = require;
      require = Object.assign((id) => {
        const module3 = __require(id);
        return module3.__esModule && module3.default ? module3.default : module3;
      }, __require);
    })();
    var __defProp2 = Object.defineProperty;
    var __getOwnPropDesc2 = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames2 = Object.getOwnPropertyNames;
    var __hasOwnProp2 = Object.prototype.hasOwnProperty;
    var __export2 = (target, all) => {
      for (var name in all)
        __defProp2(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps2 = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames2(from))
          if (!__hasOwnProp2.call(to, key) && key !== except)
            __defProp2(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc2(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps2(__defProp2({}, "__esModule", { value: true }), mod);
    var RegExp_exports = {};
    __export2(RegExp_exports, {
      escapeRegExp: () => escapeRegExp
    });
    module2.exports = __toCommonJS2(RegExp_exports);
    var __import_meta_url9 = globalThis["import.meta.url"] ?? (() => {
      if (typeof __filename !== "string") {
        return new URL(window.location.href);
      }
      return require("node:url").pathToFileURL(__filename);
    })();
    var __process9 = globalThis["process"] ?? {
      "cwd": () => "/",
      "env": {},
      "platform": "android"
    };
    function escapeRegExp(str) {
      return str.replaceAll(/[.*+?^${}()|[\]\\]/g, "\\$&");
    }
  }
});

// node_modules/obsidian-dev-utils/dist/lib/ValueProvider.cjs
var require_ValueProvider = __commonJS({
  "node_modules/obsidian-dev-utils/dist/lib/ValueProvider.cjs"(exports2, module2) {
    (function patchRequireEsmDefault() {
      const __require = require;
      require = Object.assign((id) => {
        const module3 = __require(id);
        return module3.__esModule && module3.default ? module3.default : module3;
      }, __require);
    })();
    var __defProp2 = Object.defineProperty;
    var __getOwnPropDesc2 = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames2 = Object.getOwnPropertyNames;
    var __hasOwnProp2 = Object.prototype.hasOwnProperty;
    var __export2 = (target, all) => {
      for (var name in all)
        __defProp2(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps2 = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames2(from))
          if (!__hasOwnProp2.call(to, key) && key !== except)
            __defProp2(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc2(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps2(__defProp2({}, "__esModule", { value: true }), mod);
    var ValueProvider_exports = {};
    __export2(ValueProvider_exports, {
      resolveValue: () => resolveValue
    });
    module2.exports = __toCommonJS2(ValueProvider_exports);
    var __import_meta_url9 = globalThis["import.meta.url"] ?? (() => {
      if (typeof __filename !== "string") {
        return new URL(window.location.href);
      }
      return require("node:url").pathToFileURL(__filename);
    })();
    var __process9 = globalThis["process"] ?? {
      "cwd": () => "/",
      "env": {},
      "platform": "android"
    };
    function isFunction(value) {
      return typeof value === "function";
    }
    async function resolveValue(provider, ...args) {
      if (isFunction(provider)) {
        return await provider(...args);
      } else {
        return provider;
      }
    }
  }
});

// node_modules/obsidian-dev-utils/dist/lib/String.cjs
var require_String = __commonJS({
  "node_modules/obsidian-dev-utils/dist/lib/String.cjs"(exports2, module2) {
    (function patchRequireEsmDefault() {
      const __require = require;
      require = Object.assign((id) => {
        const module3 = __require(id);
        return module3.__esModule && module3.default ? module3.default : module3;
      }, __require);
    })();
    var __defProp2 = Object.defineProperty;
    var __getOwnPropDesc2 = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames2 = Object.getOwnPropertyNames;
    var __hasOwnProp2 = Object.prototype.hasOwnProperty;
    var __export2 = (target, all) => {
      for (var name in all)
        __defProp2(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps2 = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames2(from))
          if (!__hasOwnProp2.call(to, key) && key !== except)
            __defProp2(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc2(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps2(__defProp2({}, "__esModule", { value: true }), mod);
    var String_exports = {};
    __export2(String_exports, {
      ensureEndsWith: () => ensureEndsWith,
      ensureStartsWith: () => ensureStartsWith,
      escape: () => escape,
      makeValidVariableName: () => makeValidVariableName,
      normalize: () => normalize,
      replace: () => replace,
      replaceAllAsync: () => replaceAllAsync,
      trimEnd: () => trimEnd,
      trimStart: () => trimStart,
      unescape: () => unescape
    });
    module2.exports = __toCommonJS2(String_exports);
    var import_Error2 = require_Error();
    var import_RegExp = require_RegExp();
    var import_ValueProvider = require_ValueProvider();
    var __import_meta_url9 = globalThis["import.meta.url"] ?? (() => {
      if (typeof __filename !== "string") {
        return new URL(window.location.href);
      }
      return require("node:url").pathToFileURL(__filename);
    })();
    var __process9 = globalThis["process"] ?? {
      "cwd": () => "/",
      "env": {},
      "platform": "android"
    };
    var ESCAPE_MAP = {
      "\\": "\\\\",
      '"': '\\"',
      "'": "\\'",
      "\n": "\\n",
      "\r": "\\r",
      "	": "\\t",
      "\b": "\\b",
      "\f": "\\f"
    };
    var UNESCAPE_MAP = {};
    for (const [key, value] of Object.entries(ESCAPE_MAP)) {
      UNESCAPE_MAP[value] = key;
    }
    function trimStart(str, prefix, validate) {
      if (str.startsWith(prefix)) {
        return str.slice(prefix.length);
      }
      if (validate) {
        throw new Error(`String ${str} does not start with prefix ${prefix}`);
      }
      return str;
    }
    function trimEnd(str, suffix, validate) {
      if (str.endsWith(suffix)) {
        return str.slice(0, -suffix.length);
      }
      if (validate) {
        throw new Error(`String ${str} does not end with suffix ${suffix}`);
      }
      return str;
    }
    function normalize(str) {
      return str.replace(/\u00A0|\u202F/g, " ").normalize("NFC");
    }
    async function replaceAllAsync(str, searchValue, replacer) {
      const replacementPromises = [];
      str.replaceAll(searchValue, (substring, ...args) => {
        replacementPromises.push((0, import_ValueProvider.resolveValue)(replacer, substring, ...args));
        return substring;
      });
      const replacements = await Promise.all(replacementPromises);
      return str.replaceAll(searchValue, () => replacements.shift() ?? (0, import_Error2.throwExpression)(new Error("Unexpected empty replacement")));
    }
    function makeValidVariableName(str) {
      return str.replace(/[^a-zA-Z0-9_]/g, "_");
    }
    function ensureStartsWith(str, prefix) {
      return str.startsWith(prefix) ? str : prefix + str;
    }
    function ensureEndsWith(str, suffix) {
      return str.endsWith(suffix) ? str : str + suffix;
    }
    function escape(str) {
      return replace(str, ESCAPE_MAP);
    }
    function unescape(str) {
      return replace(str, UNESCAPE_MAP);
    }
    function replace(str, replacementsMap) {
      const regExp = new RegExp(Object.keys(replacementsMap).map((source) => (0, import_RegExp.escapeRegExp)(source)).join("|"), "g");
      return str.replaceAll(regExp, (source) => replacementsMap[source] ?? (0, import_Error2.throwExpression)(new Error(`Unexpected replacement source: ${source}`)));
    }
  }
});

// node_modules/obsidian-dev-utils/dist/lib/Path.cjs
var require_Path = __commonJS({
  "node_modules/obsidian-dev-utils/dist/lib/Path.cjs"(exports2, module2) {
    (function patchRequireEsmDefault() {
      const __require = require;
      require = Object.assign((id) => {
        const module3 = __require(id);
        return module3.__esModule && module3.default ? module3.default : module3;
      }, __require);
    })();
    var __create2 = Object.create;
    var __defProp2 = Object.defineProperty;
    var __getOwnPropDesc2 = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames2 = Object.getOwnPropertyNames;
    var __getProtoOf2 = Object.getPrototypeOf;
    var __hasOwnProp2 = Object.prototype.hasOwnProperty;
    var __export2 = (target, all) => {
      for (var name in all)
        __defProp2(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps2 = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames2(from))
          if (!__hasOwnProp2.call(to, key) && key !== except)
            __defProp2(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc2(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toESM2 = (mod, isNodeMode, target) => (target = mod != null ? __create2(__getProtoOf2(mod)) : {}, __copyProps2(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp2(target, "default", { value: mod, enumerable: true }) : target,
      mod
    ));
    var __toCommonJS2 = (mod) => __copyProps2(__defProp2({}, "__esModule", { value: true }), mod);
    var Path_exports = {};
    __export2(Path_exports, {
      basename: () => basename,
      delimiter: () => delimiter,
      dirname: () => dirname,
      extname: () => extname,
      format: () => format,
      getDirname: () => getDirname,
      getFilename: () => getFilename,
      isAbsolute: () => isAbsolute,
      join: () => join,
      makeFileName: () => makeFileName,
      normalize: () => normalize,
      normalizeIfRelative: () => normalizeIfRelative,
      parse: () => parse,
      posix: () => posix,
      relative: () => relative,
      resolve: () => resolve,
      sep: () => sep,
      toPosixBuffer: () => toPosixBuffer,
      toPosixPath: () => toPosixPath
    });
    module2.exports = __toCommonJS2(Path_exports);
    var import_path_browserify = __toESM2(require_path_browserify(), 1);
    var import_String = require_String();
    var __import_meta_url9 = globalThis["import.meta.url"] ?? (() => {
      if (typeof __filename !== "string") {
        return new URL(window.location.href);
      }
      return require("node:url").pathToFileURL(__filename);
    })();
    var __process9 = globalThis["process"] ?? {
      "cwd": () => "/",
      "env": {},
      "platform": "android"
    };
    var WINDOWS_POSIX_LIKE_PATH_REG_EXP = /[a-zA-Z]:\/[^:]*$/;
    var posix = import_path_browserify.default.posix;
    var delimiter = posix.delimiter;
    var sep = import_path_browserify.default.posix.sep;
    var basename = posix.basename;
    var dirname = posix.dirname;
    var extname = posix.extname;
    var format = posix.format;
    function isAbsolute(path2) {
      return posix.isAbsolute(path2) || WINDOWS_POSIX_LIKE_PATH_REG_EXP.exec(path2)?.[0] === path2;
    }
    var join = posix.join;
    var normalize = posix.normalize;
    var parse = posix.parse;
    var relative = posix.relative;
    function resolve(...pathSegments) {
      let path2 = posix.resolve(...pathSegments);
      path2 = toPosixPath(path2);
      const match = WINDOWS_POSIX_LIKE_PATH_REG_EXP.exec(path2);
      return match?.[0] ?? path2;
    }
    function toPosixPath(path2) {
      return path2.replace(/\\/g, "/");
    }
    function toPosixBuffer(buffer) {
      return Buffer.from(toPosixPath(buffer.toString()));
    }
    function getFilename(importMetaUrl) {
      return resolve(new URL(importMetaUrl).pathname);
    }
    function getDirname(importMetaUrl) {
      return dirname(getFilename(importMetaUrl));
    }
    function normalizeIfRelative(path2) {
      if (path2.startsWith("/") || path2.includes(":")) {
        return path2;
      }
      return (0, import_String.ensureStartsWith)(path2, "./");
    }
    function makeFileName(fileName, extension) {
      return extension ? `${fileName}.${extension}` : fileName;
    }
  }
});

// node_modules/obsidian-dev-utils/dist/lib/obsidian/FileSystem.cjs
var require_FileSystem = __commonJS({
  "node_modules/obsidian-dev-utils/dist/lib/obsidian/FileSystem.cjs"(exports2, module2) {
    (function patchRequireEsmDefault() {
      const __require = require;
      require = Object.assign((id) => {
        const module3 = __require(id);
        return module3.__esModule && module3.default ? module3.default : module3;
      }, __require);
    })();
    var __defProp2 = Object.defineProperty;
    var __getOwnPropDesc2 = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames2 = Object.getOwnPropertyNames;
    var __hasOwnProp2 = Object.prototype.hasOwnProperty;
    var __export2 = (target, all) => {
      for (var name in all)
        __defProp2(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps2 = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames2(from))
          if (!__hasOwnProp2.call(to, key) && key !== except)
            __defProp2(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc2(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps2(__defProp2({}, "__esModule", { value: true }), mod);
    var FileSystem_exports = {};
    __export2(FileSystem_exports, {
      CANVAS_FILE_EXTENSION: () => CANVAS_FILE_EXTENSION,
      MARKDOWN_FILE_EXTENSION: () => MARKDOWN_FILE_EXTENSION2,
      checkExtension: () => checkExtension,
      getAbstractFile: () => getAbstractFile,
      getAbstractFileOrNull: () => getAbstractFileOrNull,
      getFile: () => getFile,
      getFileOrNull: () => getFileOrNull,
      getFolder: () => getFolder,
      getFolderOrNull: () => getFolderOrNull,
      getMarkdownFiles: () => getMarkdownFiles,
      getOrCreateFile: () => getOrCreateFile,
      getOrCreateFolder: () => getOrCreateFolder,
      getPath: () => getPath,
      isCanvasFile: () => isCanvasFile,
      isFile: () => isFile,
      isFolder: () => isFolder,
      isMarkdownFile: () => isMarkdownFile2,
      isNote: () => isNote,
      trimMarkdownExtension: () => trimMarkdownExtension
    });
    module2.exports = __toCommonJS2(FileSystem_exports);
    var import_obsidian5 = require("obsidian");
    var import_implementations = require_implementations();
    var import_Path = require_Path();
    var import_String = require_String();
    var __import_meta_url9 = globalThis["import.meta.url"] ?? (() => {
      if (typeof __filename !== "string") {
        return new URL(window.location.href);
      }
      return require("node:url").pathToFileURL(__filename);
    })();
    var __process9 = globalThis["process"] ?? {
      "cwd": () => "/",
      "env": {},
      "platform": "android"
    };
    var MARKDOWN_FILE_EXTENSION2 = "md";
    var CANVAS_FILE_EXTENSION = "canvas";
    function getAbstractFile(app, pathOrFile, insensitive) {
      const file = getAbstractFileOrNull(app, pathOrFile, insensitive);
      if (!file) {
        throw new Error(`Abstract file not found: ${pathOrFile}`);
      }
      return file;
    }
    function getAbstractFileOrNull(app, pathOrFile, insensitive) {
      if (pathOrFile === null) {
        return null;
      }
      if (pathOrFile === "." || pathOrFile === "") {
        return app.vault.getRoot();
      }
      if (pathOrFile instanceof import_obsidian5.TAbstractFile) {
        return pathOrFile;
      }
      if (insensitive) {
        return app.vault.getAbstractFileByPathInsensitive(pathOrFile);
      }
      return app.vault.getAbstractFileByPath(pathOrFile);
    }
    function getFile(app, pathOrFile, allowNonExisting, insensitive) {
      let file = getFileOrNull(app, pathOrFile, insensitive);
      if (!file) {
        if (allowNonExisting) {
          file = (0, import_implementations.createTFileInstance)(app.vault, pathOrFile);
        } else {
          throw new Error(`File not found: ${pathOrFile}`);
        }
      }
      return file;
    }
    function getFileOrNull(app, pathOrFile, insensitive) {
      const file = getAbstractFileOrNull(app, pathOrFile, insensitive);
      if (file instanceof import_obsidian5.TFile) {
        return file;
      }
      return null;
    }
    function getFolder(app, pathOrFolder, allowNonExisting, insensitive) {
      let folder = getFolderOrNull(app, pathOrFolder, insensitive);
      if (!folder) {
        if (allowNonExisting) {
          folder = (0, import_implementations.createTFolderInstance)(app.vault, pathOrFolder);
        } else {
          throw new Error(`Folder not found: ${pathOrFolder}`);
        }
      }
      return folder;
    }
    function getFolderOrNull(app, pathOrFolder, insensitive) {
      const folder = getAbstractFileOrNull(app, pathOrFolder, insensitive);
      if (folder instanceof import_obsidian5.TFolder) {
        return folder;
      }
      return null;
    }
    function getMarkdownFiles(app, pathOrFolder, isRecursive) {
      const folder = getFolder(app, pathOrFolder);
      let markdownFiles = [];
      if (!isRecursive) {
        markdownFiles = folder.children.filter((file) => isMarkdownFile2(file));
      } else {
        import_obsidian5.Vault.recurseChildren(folder, (abstractFile) => {
          if (isMarkdownFile2(abstractFile)) {
            markdownFiles.push(abstractFile);
          }
        });
      }
      markdownFiles = markdownFiles.sort((a, b) => a.path.localeCompare(b.path));
      return markdownFiles;
    }
    function isFile(file) {
      return file instanceof import_obsidian5.TFile;
    }
    function isFolder(file) {
      return file instanceof import_obsidian5.TFolder;
    }
    function isNote(file) {
      return isMarkdownFile2(file) || isCanvasFile(file);
    }
    function isMarkdownFile2(pathOrFile) {
      return checkExtension(pathOrFile, MARKDOWN_FILE_EXTENSION2);
    }
    function isCanvasFile(pathOrFile) {
      return checkExtension(pathOrFile, CANVAS_FILE_EXTENSION);
    }
    function checkExtension(pathOrFile, extension) {
      if (pathOrFile === null) {
        return false;
      }
      return (0, import_Path.extname)(getPath(pathOrFile)).toLowerCase().slice(1) === extension.toLowerCase();
    }
    function trimMarkdownExtension(file) {
      if (!isMarkdownFile2(file)) {
        return file.path;
      }
      return (0, import_String.trimEnd)(file.path, "." + MARKDOWN_FILE_EXTENSION2);
    }
    function getPath(pathOrFile) {
      return pathOrFile instanceof import_obsidian5.TAbstractFile ? pathOrFile.path : pathOrFile;
    }
    async function getOrCreateFile(app, path) {
      const file = getFileOrNull(app, path);
      if (file) {
        return file;
      }
      const folderPath = (0, import_implementations.parentFolderPath)(path);
      await getOrCreateFolder(app, folderPath);
      return await app.vault.create(path, "");
    }
    async function getOrCreateFolder(app, path) {
      const folder = getFolderOrNull(app, path);
      if (folder) {
        return folder;
      }
      return await app.vault.createFolder(path);
    }
  }
});

// node_modules/obsidian-dev-utils/dist/lib/obsidian/MetadataCache.cjs
var require_MetadataCache = __commonJS({
  "node_modules/obsidian-dev-utils/dist/lib/obsidian/MetadataCache.cjs"(exports2, module2) {
    (function patchRequireEsmDefault() {
      const __require = require;
      require = Object.assign((id) => {
        const module3 = __require(id);
        return module3.__esModule && module3.default ? module3.default : module3;
      }, __require);
    })();
    var __defProp2 = Object.defineProperty;
    var __getOwnPropDesc2 = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames2 = Object.getOwnPropertyNames;
    var __hasOwnProp2 = Object.prototype.hasOwnProperty;
    var __export2 = (target, all) => {
      for (var name in all)
        __defProp2(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps2 = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames2(from))
          if (!__hasOwnProp2.call(to, key) && key !== except)
            __defProp2(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc2(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps2(__defProp2({}, "__esModule", { value: true }), mod);
    var MetadataCache_exports = {};
    __export2(MetadataCache_exports, {
      ensureMetadataCacheReady: () => ensureMetadataCacheReady,
      getAllLinks: () => getAllLinks2,
      getBacklinksForFileSafe: () => getBacklinksForFileSafe,
      getBacklinksMap: () => getBacklinksMap,
      getCacheSafe: () => getCacheSafe2,
      getFrontMatterSafe: () => getFrontMatterSafe,
      registerFile: () => registerFile,
      tempRegisterFileAndRun: () => tempRegisterFileAndRun
    });
    module2.exports = __toCommonJS2(MetadataCache_exports);
    var import_obsidian5 = require("obsidian");
    var import_implementations = require_implementations();
    var import_Async = require_Async();
    var import_Error2 = require_Error();
    var import_FileSystem3 = require_FileSystem();
    var __import_meta_url9 = globalThis["import.meta.url"] ?? (() => {
      if (typeof __filename !== "string") {
        return new URL(window.location.href);
      }
      return require("node:url").pathToFileURL(__filename);
    })();
    var __process9 = globalThis["process"] ?? {
      "cwd": () => "/",
      "env": {},
      "platform": "android"
    };
    async function getCacheSafe2(app, fileOrPath, retryOptions = {}) {
      const DEFAULT_RETRY_OPTIONS = { timeoutInMilliseconds: 6e4 };
      const overriddenOptions = { ...DEFAULT_RETRY_OPTIONS, ...retryOptions };
      let cache = null;
      await (0, import_Async.retryWithTimeout)(async () => {
        const file = (0, import_FileSystem3.getFileOrNull)(app, fileOrPath);
        if (!file || file.deleted) {
          cache = null;
          return true;
        }
        await saveNote(app, file);
        const fileInfo = app.metadataCache.getFileInfo(file.path);
        const stat = await app.vault.adapter.stat(file.path);
        if (!fileInfo) {
          console.debug(`File cache info for ${file.path} is missing`);
          return false;
        } else if (!stat) {
          console.debug(`File stat for ${file.path} is missing`);
          return false;
        } else if (fileInfo.mtime < stat.mtime) {
          console.debug(`File cache info for ${file.path} is from ${new Date(fileInfo.mtime).toString()} which is older than the file modification timestamp ${new Date(stat.mtime).toString()}`);
          return false;
        } else {
          cache = app.metadataCache.getFileCache(file);
          if (!cache) {
            console.debug(`File cache for ${file.path} is missing`);
            return false;
          } else {
            return true;
          }
        }
      }, overriddenOptions);
      return cache;
    }
    function getAllLinks2(cache) {
      let links = [];
      if (cache.links) {
        links.push(...cache.links);
      }
      if (cache.embeds) {
        links.push(...cache.embeds);
      }
      links.sort((a, b) => a.position.start.offset - b.position.start.offset);
      links = links.filter((link, index) => {
        if (index === 0) {
          return true;
        }
        const previousLink = links[index - 1] ?? (0, import_Error2.throwExpression)(new Error("Previous link not found"));
        return link.position.start.offset !== previousLink.position.start.offset;
      });
      return links;
    }
    async function getBacklinksForFileSafe(app, pathOrFile, retryOptions = {}) {
      const DEFAULT_RETRY_OPTIONS = { timeoutInMilliseconds: 6e4 };
      const overriddenOptions = { ...DEFAULT_RETRY_OPTIONS, ...retryOptions };
      let backlinks = null;
      await (0, import_Async.retryWithTimeout)(async () => {
        const file = (0, import_FileSystem3.getFile)(app, pathOrFile);
        await ensureMetadataCacheReady(app);
        backlinks = tempRegisterFileAndRun(app, file, () => app.metadataCache.getBacklinksForFile(file));
        for (const notePath of backlinks.keys()) {
          const note = (0, import_FileSystem3.getFileOrNull)(app, notePath);
          if (!note) {
            return false;
          }
          await saveNote(app, note);
          const content = await app.vault.read(note);
          const links = backlinks.get(notePath) ?? (0, import_Error2.throwExpression)(new Error("Backlinks not found"));
          for (const link of links) {
            const actualLink = content.slice(link.position.start.offset, link.position.end.offset);
            if (actualLink !== link.original) {
              return false;
            }
          }
        }
        return true;
      }, overriddenOptions);
      return backlinks;
    }
    async function getBacklinksMap(app, pathOrFiles, retryOptions = {}) {
      const map = /* @__PURE__ */ new Map();
      for (const pathOrFile of pathOrFiles) {
        const customArrayDict = await getBacklinksForFileSafe(app, pathOrFile, retryOptions);
        for (const path of customArrayDict.keys()) {
          const mapLinks = map.get(path) ?? [];
          const pathLinks = customArrayDict.get(path) ?? [];
          mapLinks.push(...pathLinks);
          map.set(path, mapLinks);
        }
      }
      return map;
    }
    async function saveNote(app, pathOrFile) {
      if (!(0, import_FileSystem3.isMarkdownFile)(pathOrFile)) {
        return;
      }
      const path = (0, import_FileSystem3.getPath)(pathOrFile);
      for (const leaf of app.workspace.getLeavesOfType("markdown")) {
        const view = leaf.view;
        if (view.file?.path === path) {
          await view.save();
        }
      }
    }
    async function getFrontMatterSafe(app, pathOrFile) {
      const cache = await getCacheSafe2(app, pathOrFile);
      return cache?.frontmatter ?? {};
    }
    function tempRegisterFileAndRun(app, file, fn) {
      const unregister = registerFile(app, file);
      try {
        return fn();
      } finally {
        unregister();
      }
    }
    function registerFile(app, file) {
      if (!file.deleted) {
        return () => {
        };
      }
      const deletedPaths = [];
      let deletedFile = file;
      while (deletedFile.deleted) {
        deletedPaths.push(deletedFile.path);
        app.vault.fileMap[deletedFile.path] = deletedFile;
        deletedFile = deletedFile.parent ?? (0, import_FileSystem3.getFolder)(app, (0, import_implementations.parentFolderPath)(deletedFile.path), true);
      }
      if (file instanceof import_obsidian5.TFile) {
        app.metadataCache.uniqueFileLookup.add(file.name.toLowerCase(), file);
      }
      return () => {
        for (const path of deletedPaths) {
          delete app.vault.fileMap[path];
        }
        if (file instanceof import_obsidian5.TFile) {
          app.metadataCache.uniqueFileLookup.remove(file.name.toLowerCase(), file);
        }
      };
    }
    async function ensureMetadataCacheReady(app) {
      for (const [path, cache] of Object.entries(app.metadataCache.fileCache)) {
        if (!cache.hash) {
          continue;
        }
        if (app.metadataCache.metadataCache[cache.hash]) {
          continue;
        }
        await getCacheSafe2(app, path);
      }
    }
  }
});

// node_modules/obsidian-dev-utils/dist/lib/obsidian/ObsidianSettings.cjs
var require_ObsidianSettings = __commonJS({
  "node_modules/obsidian-dev-utils/dist/lib/obsidian/ObsidianSettings.cjs"(exports2, module2) {
    (function patchRequireEsmDefault() {
      const __require = require;
      require = Object.assign((id) => {
        const module3 = __require(id);
        return module3.__esModule && module3.default ? module3.default : module3;
      }, __require);
    })();
    var __defProp2 = Object.defineProperty;
    var __getOwnPropDesc2 = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames2 = Object.getOwnPropertyNames;
    var __hasOwnProp2 = Object.prototype.hasOwnProperty;
    var __export2 = (target, all) => {
      for (var name in all)
        __defProp2(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps2 = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames2(from))
          if (!__hasOwnProp2.call(to, key) && key !== except)
            __defProp2(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc2(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps2(__defProp2({}, "__esModule", { value: true }), mod);
    var ObsidianSettings_exports = {};
    __export2(ObsidianSettings_exports, {
      shouldUseRelativeLinks: () => shouldUseRelativeLinks2,
      shouldUseWikilinks: () => shouldUseWikilinks2
    });
    module2.exports = __toCommonJS2(ObsidianSettings_exports);
    var __import_meta_url9 = globalThis["import.meta.url"] ?? (() => {
      if (typeof __filename !== "string") {
        return new URL(window.location.href);
      }
      return require("node:url").pathToFileURL(__filename);
    })();
    var __process9 = globalThis["process"] ?? {
      "cwd": () => "/",
      "env": {},
      "platform": "android"
    };
    function shouldUseWikilinks2(app) {
      return !app.vault.getConfig("useMarkdownLinks");
    }
    function shouldUseRelativeLinks2(app) {
      return app.vault.getConfig("newLinkFormat") === "relative";
    }
  }
});

// node_modules/obsidian-dev-utils/dist/lib/Object.cjs
var require_Object = __commonJS({
  "node_modules/obsidian-dev-utils/dist/lib/Object.cjs"(exports2, module2) {
    (function patchRequireEsmDefault() {
      const __require = require;
      require = Object.assign((id) => {
        const module3 = __require(id);
        return module3.__esModule && module3.default ? module3.default : module3;
      }, __require);
    })();
    var __defProp2 = Object.defineProperty;
    var __getOwnPropDesc2 = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames2 = Object.getOwnPropertyNames;
    var __hasOwnProp2 = Object.prototype.hasOwnProperty;
    var __export2 = (target, all) => {
      for (var name in all)
        __defProp2(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps2 = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames2(from))
          if (!__hasOwnProp2.call(to, key) && key !== except)
            __defProp2(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc2(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps2(__defProp2({}, "__esModule", { value: true }), mod);
    var Object_exports = {};
    __export2(Object_exports, {
      deepEqual: () => deepEqual,
      getPrototypeOf: () => getPrototypeOf,
      nameof: () => nameof,
      toJson: () => toJson
    });
    module2.exports = __toCommonJS2(Object_exports);
    var import_Error2 = require_Error();
    var __import_meta_url9 = globalThis["import.meta.url"] ?? (() => {
      if (typeof __filename !== "string") {
        return new URL(window.location.href);
      }
      return require("node:url").pathToFileURL(__filename);
    })();
    var __process9 = globalThis["process"] ?? {
      "cwd": () => "/",
      "env": {},
      "platform": "android"
    };
    function deepEqual(a, b) {
      if (a === b) {
        return true;
      }
      if (typeof a !== "object" || typeof b !== "object" || a === null || b === null) {
        return false;
      }
      const keysA = Object.keys(a);
      const keysB = Object.keys(b);
      if (keysA.length !== keysB.length) {
        return false;
      }
      const aRecord = a;
      const bRecord = b;
      for (const key of keysA) {
        if (!keysB.includes(key) || !deepEqual(aRecord[key], bRecord[key])) {
          return false;
        }
      }
      return true;
    }
    function nameof(name) {
      return name;
    }
    function getPrototypeOf(instance) {
      return Object.getPrototypeOf(instance);
    }
    function toJson(value, options = {}) {
      const {
        shouldHandleFunctions = false,
        space = 2
      } = options;
      if (!shouldHandleFunctions) {
        return JSON.stringify(value, null, space);
      }
      const functionTexts = [];
      const replacer = (_, value2) => {
        if (typeof value2 === "function") {
          const index = functionTexts.length;
          functionTexts.push(value2.toString());
          return `__FUNCTION_${index.toString()}`;
        }
        return value2;
      };
      let json = JSON.stringify(value, replacer, space);
      json = json.replaceAll(/"__FUNCTION_(\d+)"/g, (_, indexStr) => functionTexts[parseInt(indexStr)] ?? (0, import_Error2.throwExpression)(new Error(`Function with index ${indexStr} not found`)));
      return json;
    }
  }
});

// node_modules/obsidian-dev-utils/dist/lib/obsidian/Vault.cjs
var require_Vault = __commonJS({
  "node_modules/obsidian-dev-utils/dist/lib/obsidian/Vault.cjs"(exports2, module2) {
    (function patchRequireEsmDefault() {
      const __require = require;
      require = Object.assign((id) => {
        const module3 = __require(id);
        return module3.__esModule && module3.default ? module3.default : module3;
      }, __require);
    })();
    var __defProp2 = Object.defineProperty;
    var __getOwnPropDesc2 = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames2 = Object.getOwnPropertyNames;
    var __hasOwnProp2 = Object.prototype.hasOwnProperty;
    var __export2 = (target, all) => {
      for (var name in all)
        __defProp2(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps2 = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames2(from))
          if (!__hasOwnProp2.call(to, key) && key !== except)
            __defProp2(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc2(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps2(__defProp2({}, "__esModule", { value: true }), mod);
    var Vault_exports = {};
    __export2(Vault_exports, {
      applyFileChanges: () => applyFileChanges2,
      copySafe: () => copySafe,
      createFolderSafe: () => createFolderSafe,
      createTempFile: () => createTempFile,
      createTempFolder: () => createTempFolder,
      deleteEmptyFolderHierarchy: () => deleteEmptyFolderHierarchy,
      deleteSafe: () => deleteSafe,
      getAvailablePath: () => getAvailablePath,
      getMarkdownFilesSorted: () => getMarkdownFilesSorted2,
      isEmptyFolder: () => isEmptyFolder,
      listSafe: () => listSafe,
      processWithRetry: () => processWithRetry,
      renameSafe: () => renameSafe
    });
    module2.exports = __toCommonJS2(Vault_exports);
    var import_obsidian5 = require("obsidian");
    var import_implementations = require_implementations();
    var import_Async = require_Async();
    var import_Error2 = require_Error();
    var import_Object = require_Object();
    var import_Path = require_Path();
    var import_ValueProvider = require_ValueProvider();
    var import_FileSystem3 = require_FileSystem();
    var import_MetadataCache2 = require_MetadataCache();
    var __import_meta_url9 = globalThis["import.meta.url"] ?? (() => {
      if (typeof __filename !== "string") {
        return new URL(window.location.href);
      }
      return require("node:url").pathToFileURL(__filename);
    })();
    var __process9 = globalThis["process"] ?? {
      "cwd": () => "/",
      "env": {},
      "platform": "android"
    };
    function getMarkdownFilesSorted2(app) {
      return app.vault.getMarkdownFiles().sort((a, b) => a.path.localeCompare(b.path));
    }
    async function processWithRetry(app, pathOrFile, newContentProvider, retryOptions = {}) {
      const file = (0, import_FileSystem3.getFile)(app, pathOrFile);
      const DEFAULT_RETRY_OPTIONS = { timeoutInMilliseconds: 6e4 };
      const overriddenOptions = { ...DEFAULT_RETRY_OPTIONS, ...retryOptions };
      await (0, import_Async.retryWithTimeout)(async () => {
        const oldContent = await app.vault.read(file);
        const newContent = await (0, import_ValueProvider.resolveValue)(newContentProvider, oldContent);
        if (newContent === null) {
          return false;
        }
        let success = true;
        await app.vault.process(file, (content) => {
          if (content !== oldContent) {
            console.warn("Content has changed since it was read. Retrying...", {
              path: file.path,
              expectedContent: oldContent,
              actualContent: content
            });
            success = false;
            return content;
          }
          return newContent;
        });
        return success;
      }, overriddenOptions);
    }
    async function applyFileChanges2(app, pathOrFile, changesProvider, retryOptions = {}) {
      const DEFAULT_RETRY_OPTIONS = { timeoutInMilliseconds: 6e4 };
      const overriddenOptions = { ...DEFAULT_RETRY_OPTIONS, ...retryOptions };
      await processWithRetry(app, pathOrFile, async (content) => {
        let changes = await (0, import_ValueProvider.resolveValue)(changesProvider);
        for (const change of changes) {
          const actualContent = content.slice(change.startIndex, change.endIndex);
          if (actualContent !== change.oldContent) {
            console.warn("Content mismatch", {
              startIndex: change.startIndex,
              endIndex: change.endIndex,
              path: (0, import_FileSystem3.getPath)(pathOrFile),
              expectedContent: change.oldContent,
              actualContent
            });
            return null;
          }
        }
        changes.sort((a, b) => a.startIndex - b.startIndex);
        changes = changes.filter((change, index) => {
          if (index === 0) {
            return true;
          }
          return !(0, import_Object.deepEqual)(change, changes[index - 1]);
        });
        for (let i = 1; i < changes.length; i++) {
          const change = changes[i] ?? (0, import_Error2.throwExpression)(new Error("Change not found"));
          const previousChange = changes[i - 1] ?? (0, import_Error2.throwExpression)(new Error("Previous change not found"));
          if (previousChange.endIndex > change.startIndex) {
            console.warn("Overlapping changes", {
              previousChange,
              change
            });
            return null;
          }
        }
        let newContent = "";
        let lastIndex = 0;
        for (const change of changes) {
          newContent += content.slice(lastIndex, change.startIndex);
          newContent += change.newContent;
          lastIndex = change.endIndex;
        }
        newContent += content.slice(lastIndex);
        return newContent;
      }, overriddenOptions);
    }
    async function deleteSafe(app, pathOrFile, deletedNotePath, shouldReportUsedAttachments, shouldDeleteEmptyFolders) {
      const file = (0, import_FileSystem3.getAbstractFileOrNull)(app, pathOrFile);
      if (!file) {
        return false;
      }
      let canDelete = file instanceof import_obsidian5.TFile || (shouldDeleteEmptyFolders ?? true);
      if (file instanceof import_obsidian5.TFile) {
        const backlinks = await (0, import_MetadataCache2.getBacklinksForFileSafe)(app, file);
        if (deletedNotePath) {
          backlinks.removeKey(deletedNotePath);
        }
        if (backlinks.count() !== 0) {
          if (shouldReportUsedAttachments) {
            new import_obsidian5.Notice(`Attachment ${file.path} is still used by other notes. It will not be deleted.`);
          }
          canDelete = false;
        }
      } else if (file instanceof import_obsidian5.TFolder) {
        for (const child of file.children) {
          canDelete &&= await deleteSafe(app, child.path, deletedNotePath, shouldReportUsedAttachments);
        }
        canDelete &&= await isEmptyFolder(app, file);
      }
      if (canDelete) {
        try {
          await app.fileManager.trashFile(file);
        } catch (e) {
          if (await app.vault.exists(file.path)) {
            (0, import_Error2.printError)(new Error(`Failed to delete ${file.path}`, { cause: e }));
            canDelete = false;
          }
        }
      }
      return canDelete;
    }
    async function createFolderSafe(app, path) {
      if (await app.vault.adapter.exists(path)) {
        return false;
      }
      try {
        await app.vault.createFolder(path);
        return true;
      } catch (e) {
        if (!await app.vault.exists(path)) {
          throw e;
        }
        return true;
      }
    }
    async function listSafe(app, pathOrFolder) {
      const path = (0, import_FileSystem3.getPath)(pathOrFolder);
      const EMPTY = { files: [], folders: [] };
      if ((await app.vault.adapter.stat(path))?.type !== "folder") {
        return EMPTY;
      }
      try {
        return await app.vault.adapter.list(path);
      } catch (e) {
        if (await app.vault.exists(path)) {
          throw e;
        }
        return EMPTY;
      }
    }
    async function deleteEmptyFolderHierarchy(app, pathOrFolder) {
      let folder = (0, import_FileSystem3.getFolderOrNull)(app, pathOrFolder);
      while (folder) {
        if (!await isEmptyFolder(app, folder)) {
          return;
        }
        const parent = folder.parent;
        await deleteSafe(app, folder.path);
        folder = parent;
      }
    }
    async function createTempFile(app, path) {
      let file = (0, import_FileSystem3.getFileOrNull)(app, path);
      if (file) {
        return async () => {
        };
      }
      const folderCleanup = await createTempFolder(app, (0, import_implementations.parentFolderPath)(path));
      try {
        await app.vault.create(path, "");
      } catch (e) {
        if (!await app.vault.exists(path)) {
          throw e;
        }
      }
      file = (0, import_FileSystem3.getFile)(app, path);
      return async () => {
        if (!file.deleted) {
          await app.fileManager.trashFile(file);
        }
        await folderCleanup();
      };
    }
    async function createTempFolder(app, path) {
      let folder = (0, import_FileSystem3.getFolderOrNull)(app, path);
      if (folder) {
        return async () => {
        };
      }
      const dirPath = (0, import_implementations.parentFolderPath)(path);
      await createTempFolder(app, dirPath);
      const folderCleanup = await createTempFolder(app, (0, import_implementations.parentFolderPath)(path));
      await createFolderSafe(app, path);
      folder = (0, import_FileSystem3.getFolder)(app, path);
      return async () => {
        if (!folder.deleted) {
          await app.fileManager.trashFile(folder);
        }
        await folderCleanup();
      };
    }
    async function isEmptyFolder(app, pathOrFolder) {
      const listedFiles = await listSafe(app, (0, import_FileSystem3.getPath)(pathOrFolder));
      return listedFiles.files.length === 0 && listedFiles.folders.length === 0;
    }
    async function renameSafe(app, oldPathOrFile, newPath) {
      const file = (0, import_FileSystem3.getFile)(app, oldPathOrFile);
      if (file.path === newPath) {
        return newPath;
      }
      const newFolderPath = (0, import_implementations.parentFolderPath)(newPath);
      await createFolderSafe(app, newFolderPath);
      const newAvailablePath = getAvailablePath(app, newPath);
      try {
        await app.vault.rename(file, newAvailablePath);
      } catch (e) {
        if (!await app.vault.exists(newAvailablePath) || await app.vault.exists(file.path)) {
          throw e;
        }
      }
      return newAvailablePath;
    }
    async function copySafe(app, oldPathOrFile, newPath) {
      const file = (0, import_FileSystem3.getFile)(app, oldPathOrFile);
      const newFolderPath = (0, import_implementations.parentFolderPath)(newPath);
      await createFolderSafe(app, newFolderPath);
      const newAvailablePath = getAvailablePath(app, newPath);
      try {
        await app.vault.copy(file, newAvailablePath);
      } catch (e) {
        if (!await app.vault.exists(newAvailablePath)) {
          throw e;
        }
      }
      return newAvailablePath;
    }
    function getAvailablePath(app, path) {
      const ext = (0, import_Path.extname)(path);
      return app.vault.getAvailablePath((0, import_Path.join)((0, import_Path.dirname)(path), (0, import_Path.basename)(path, ext)), ext.slice(1));
    }
  }
});

// node_modules/obsidian-dev-utils/dist/lib/obsidian/Link.cjs
var require_Link = __commonJS({
  "node_modules/obsidian-dev-utils/dist/lib/obsidian/Link.cjs"(exports2, module2) {
    (function patchRequireEsmDefault() {
      const __require = require;
      require = Object.assign((id) => {
        const module3 = __require(id);
        return module3.__esModule && module3.default ? module3.default : module3;
      }, __require);
    })();
    var __defProp2 = Object.defineProperty;
    var __getOwnPropDesc2 = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames2 = Object.getOwnPropertyNames;
    var __hasOwnProp2 = Object.prototype.hasOwnProperty;
    var __export2 = (target, all) => {
      for (var name in all)
        __defProp2(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps2 = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames2(from))
          if (!__hasOwnProp2.call(to, key) && key !== except)
            __defProp2(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc2(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps2(__defProp2({}, "__esModule", { value: true }), mod);
    var Link_exports = {};
    __export2(Link_exports, {
      convertLink: () => convertLink2,
      editLinks: () => editLinks,
      extractLinkFile: () => extractLinkFile,
      generateMarkdownLink: () => generateMarkdownLink2,
      shouldResetAlias: () => shouldResetAlias,
      splitSubpath: () => splitSubpath2,
      testAngleBrackets: () => testAngleBrackets,
      testEmbed: () => testEmbed,
      testLeadingDot: () => testLeadingDot,
      testWikilink: () => testWikilink,
      updateLink: () => updateLink,
      updateLinksInFile: () => updateLinksInFile2
    });
    module2.exports = __toCommonJS2(Link_exports);
    var import_obsidian5 = require("obsidian");
    var import_Path = require_Path();
    var import_String = require_String();
    var import_FileSystem3 = require_FileSystem();
    var import_MetadataCache2 = require_MetadataCache();
    var import_ObsidianSettings3 = require_ObsidianSettings();
    var import_Vault2 = require_Vault();
    var __import_meta_url9 = globalThis["import.meta.url"] ?? (() => {
      if (typeof __filename !== "string") {
        return new URL(window.location.href);
      }
      return require("node:url").pathToFileURL(__filename);
    })();
    var __process9 = globalThis["process"] ?? {
      "cwd": () => "/",
      "env": {},
      "platform": "android"
    };
    var SPECIAL_LINK_SYMBOLS_REGEXP = /[\\\x00\x08\x0B\x0C\x0E-\x1F ]/g;
    function splitSubpath2(link) {
      const SUBPATH_SEPARATOR = "#";
      const [linkPath = "", subpath] = (0, import_String.normalize)(link).split(SUBPATH_SEPARATOR);
      return {
        linkPath,
        subpath: subpath ? SUBPATH_SEPARATOR + subpath : void 0
      };
    }
    async function updateLinksInFile2(options) {
      const {
        app,
        pathOrFile,
        oldPathOrFile,
        renameMap,
        forceMarkdownLinks,
        embedOnlyLinks,
        shouldUpdateFilenameAlias
      } = options;
      await editLinks(app, pathOrFile, (link) => {
        const isEmbedLink = testEmbed(link.original);
        if (embedOnlyLinks !== void 0 && embedOnlyLinks !== isEmbedLink) {
          return;
        }
        return convertLink2({
          app,
          link,
          sourcePathOrFile: pathOrFile,
          oldPathOrFile,
          renameMap,
          forceMarkdownLinks,
          shouldUpdateFilenameAlias
        });
      });
    }
    function convertLink2(options) {
      return updateLink({
        app: options.app,
        link: options.link,
        pathOrFile: extractLinkFile(options.app, options.link, options.sourcePathOrFile),
        oldPathOrFile: options.oldPathOrFile,
        sourcePathOrFile: options.sourcePathOrFile,
        renameMap: options.renameMap,
        forceMarkdownLinks: options.forceMarkdownLinks,
        shouldUpdateFilenameAlias: options.shouldUpdateFilenameAlias
      });
    }
    function extractLinkFile(app, link, oldPathOrFile) {
      const { linkPath } = splitSubpath2(link.link);
      return app.metadataCache.getFirstLinkpathDest(linkPath, (0, import_FileSystem3.getPath)(oldPathOrFile));
    }
    function updateLink(options) {
      const {
        app,
        link,
        pathOrFile,
        oldPathOrFile,
        sourcePathOrFile,
        renameMap,
        forceMarkdownLinks,
        shouldUpdateFilenameAlias
      } = options;
      if (!pathOrFile) {
        return link.original;
      }
      let file = (0, import_FileSystem3.getFile)(app, pathOrFile);
      const oldPath = (0, import_FileSystem3.getPath)(oldPathOrFile ?? sourcePathOrFile);
      const isWikilink = testWikilink(link.original) && forceMarkdownLinks !== true;
      const { subpath } = splitSubpath2(link.link);
      const newPath = renameMap?.get(file.path);
      let alias = shouldResetAlias({
        app,
        displayText: link.displayText,
        pathOrFile,
        otherPathOrFiles: [oldPath, newPath],
        sourcePathOrFile,
        isWikilink
      }) ? void 0 : link.displayText;
      if (shouldUpdateFilenameAlias ?? true) {
        if (alias?.toLowerCase() === (0, import_Path.basename)(oldPath, (0, import_Path.extname)(oldPath)).toLowerCase()) {
          alias = file.basename;
        } else if (alias?.toLowerCase() === (0, import_Path.basename)(oldPath).toLowerCase()) {
          alias = file.name;
        }
      }
      if (newPath) {
        file = (0, import_FileSystem3.getFile)(app, newPath, true);
      }
      const newLink = generateMarkdownLink2({
        app,
        pathOrFile: file,
        sourcePathOrFile,
        subpath,
        alias,
        isWikilink: forceMarkdownLinks ? false : void 0,
        originalLink: link.original
      });
      return newLink;
    }
    function shouldResetAlias(options) {
      const {
        app,
        displayText,
        pathOrFile,
        otherPathOrFiles,
        sourcePathOrFile,
        isWikilink
      } = options;
      if (isWikilink === false) {
        return false;
      }
      const file = (0, import_FileSystem3.getFile)(app, pathOrFile);
      if (!displayText) {
        return true;
      }
      const sourcePath = (0, import_FileSystem3.getPath)(sourcePathOrFile);
      const sourceDir = (0, import_Path.dirname)(sourcePath);
      const aliasesToReset = /* @__PURE__ */ new Set();
      for (const pathOrFile2 of [file.path, ...otherPathOrFiles]) {
        if (!pathOrFile2) {
          continue;
        }
        const path = (0, import_FileSystem3.getPath)(pathOrFile2);
        aliasesToReset.add(path);
        aliasesToReset.add((0, import_Path.basename)(path));
        aliasesToReset.add((0, import_Path.relative)(sourceDir, path));
      }
      aliasesToReset.add(app.metadataCache.fileToLinktext(file, sourcePath, false));
      const cleanDisplayText = (0, import_obsidian5.normalizePath)(displayText.split(" > ")[0] ?? "").replace(/^\.\//, "").toLowerCase();
      for (const alias of aliasesToReset) {
        if (alias.toLowerCase() === cleanDisplayText) {
          return true;
        }
        const dir = (0, import_Path.dirname)(alias);
        const base = (0, import_Path.basename)(alias, (0, import_Path.extname)(alias));
        if ((0, import_Path.join)(dir, base).toLowerCase() === cleanDisplayText) {
          return true;
        }
      }
      return false;
    }
    function generateMarkdownLink2(options) {
      const { app } = options;
      const configurableDefaultOptionsFn = app.fileManager.generateMarkdownLink.defaultOptionsFn ?? (() => ({}));
      const configurableDefaultOptions = configurableDefaultOptionsFn();
      const DEFAULT_OPTIONS = {
        allowEmptyEmbedAlias: true
      };
      options = { ...DEFAULT_OPTIONS, ...configurableDefaultOptions, ...options };
      const file = (0, import_FileSystem3.getFile)(app, options.pathOrFile, options.allowNonExistingFile);
      return (0, import_MetadataCache2.tempRegisterFileAndRun)(app, file, () => {
        const sourcePath = (0, import_FileSystem3.getPath)(options.sourcePathOrFile);
        const subpath = options.subpath ?? "";
        let alias = options.alias ?? "";
        const isEmbed = options.isEmbed ?? (options.originalLink ? testEmbed(options.originalLink) : void 0) ?? !(0, import_FileSystem3.isMarkdownFile)(file);
        const isWikilink = options.isWikilink ?? (options.originalLink ? testWikilink(options.originalLink) : void 0) ?? (0, import_ObsidianSettings3.shouldUseWikilinks)(app);
        const forceRelativePath = options.forceRelativePath ?? (0, import_ObsidianSettings3.shouldUseRelativeLinks)(app);
        const useLeadingDot = options.useLeadingDot ?? (options.originalLink ? testLeadingDot(options.originalLink) : void 0) ?? false;
        const useAngleBrackets = options.useAngleBrackets ?? (options.originalLink ? testAngleBrackets(options.originalLink) : void 0) ?? false;
        let linkText = file.path === sourcePath && subpath ? subpath : forceRelativePath ? (0, import_Path.relative)((0, import_Path.dirname)(sourcePath), isWikilink ? (0, import_FileSystem3.trimMarkdownExtension)(file) : file.path) + subpath : app.metadataCache.fileToLinktext(file, sourcePath, isWikilink) + subpath;
        if (forceRelativePath && useLeadingDot && !linkText.startsWith(".") && !linkText.startsWith("#")) {
          linkText = "./" + linkText;
        }
        const embedPrefix = isEmbed ? "!" : "";
        if (!isWikilink) {
          if (useAngleBrackets) {
            linkText = `<${linkText}>`;
          } else {
            linkText = linkText.replace(SPECIAL_LINK_SYMBOLS_REGEXP, function(specialLinkSymbol) {
              return encodeURIComponent(specialLinkSymbol);
            });
          }
          if (!alias && (!isEmbed || !options.allowEmptyEmbedAlias)) {
            alias = !options.includeAttachmentExtensionToEmbedAlias || (0, import_FileSystem3.isMarkdownFile)(file) ? file.basename : file.name;
          }
          return `${embedPrefix}[${alias}](${linkText})`;
        } else {
          if (alias && alias.toLowerCase() === linkText.toLowerCase()) {
            linkText = alias;
            alias = "";
          }
          const aliasPart = alias ? `|${alias}` : "";
          return `${embedPrefix}[[${linkText}${aliasPart}]]`;
        }
      });
    }
    async function editLinks(app, pathOrFile, linkConverter, retryOptions = {}) {
      await (0, import_Vault2.applyFileChanges)(app, pathOrFile, async () => {
        const cache = await (0, import_MetadataCache2.getCacheSafe)(app, pathOrFile);
        if (!cache) {
          return [];
        }
        const changes = [];
        for (const link of (0, import_MetadataCache2.getAllLinks)(cache)) {
          const newContent = await linkConverter(link);
          if (newContent === void 0) {
            continue;
          }
          changes.push({
            startIndex: link.position.start.offset,
            endIndex: link.position.end.offset,
            oldContent: link.original,
            newContent
          });
        }
        return changes;
      }, retryOptions);
    }
    function testEmbed(link) {
      return link.startsWith("![");
    }
    function testWikilink(link) {
      return link.includes("[[");
    }
    function testLeadingDot(link) {
      return link.includes("[[./") || link.includes("](./") || link.includes("](<./");
    }
    function testAngleBrackets(link) {
      return link.includes("](<");
    }
  }
});

// node_modules/obsidian-dev-utils/dist/lib/obsidian/Plugin/PluginSettings.cjs
var require_PluginSettings = __commonJS({
  "node_modules/obsidian-dev-utils/dist/lib/obsidian/Plugin/PluginSettings.cjs"(exports2, module2) {
    (function patchRequireEsmDefault() {
      const __require = require;
      require = Object.assign((id) => {
        const module3 = __require(id);
        return module3.__esModule && module3.default ? module3.default : module3;
      }, __require);
    })();
    var __defProp2 = Object.defineProperty;
    var __getOwnPropDesc2 = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames2 = Object.getOwnPropertyNames;
    var __hasOwnProp2 = Object.prototype.hasOwnProperty;
    var __export2 = (target, all) => {
      for (var name in all)
        __defProp2(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps2 = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames2(from))
          if (!__hasOwnProp2.call(to, key) && key !== except)
            __defProp2(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc2(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps2(__defProp2({}, "__esModule", { value: true }), mod);
    var PluginSettings_exports = {};
    __export2(PluginSettings_exports, {
      clonePluginSettings: () => clonePluginSettings,
      loadPluginSettings: () => loadPluginSettings
    });
    module2.exports = __toCommonJS2(PluginSettings_exports);
    var __import_meta_url9 = globalThis["import.meta.url"] ?? (() => {
      if (typeof __filename !== "string") {
        return new URL(window.location.href);
      }
      return require("node:url").pathToFileURL(__filename);
    })();
    var __process9 = globalThis["process"] ?? {
      "cwd": () => "/",
      "env": {},
      "platform": "android"
    };
    function loadPluginSettings(defaultPluginSettingsFactory, data) {
      const defaultPluginSettings = defaultPluginSettingsFactory();
      if (data && typeof data === "object") {
        const record = data;
        for (const [key, value] of Object.entries(record)) {
          if (key in defaultPluginSettings) {
            defaultPluginSettings[key] = value;
          }
        }
      }
      return defaultPluginSettings;
    }
    function clonePluginSettings(defaultPluginSettingsFactory, settings) {
      return loadPluginSettings(defaultPluginSettingsFactory, settings);
    }
  }
});

// node_modules/obsidian-dev-utils/dist/lib/obsidian/Plugin/PluginBase.cjs
var require_PluginBase = __commonJS({
  "node_modules/obsidian-dev-utils/dist/lib/obsidian/Plugin/PluginBase.cjs"(exports2, module2) {
    (function patchRequireEsmDefault() {
      const __require = require;
      require = Object.assign((id) => {
        const module3 = __require(id);
        return module3.__esModule && module3.default ? module3.default : module3;
      }, __require);
    })();
    var __defProp2 = Object.defineProperty;
    var __getOwnPropDesc2 = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames2 = Object.getOwnPropertyNames;
    var __hasOwnProp2 = Object.prototype.hasOwnProperty;
    var __export2 = (target, all) => {
      for (var name in all)
        __defProp2(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps2 = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames2(from))
          if (!__hasOwnProp2.call(to, key) && key !== except)
            __defProp2(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc2(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps2(__defProp2({}, "__esModule", { value: true }), mod);
    var PluginBase_exports = {};
    __export2(PluginBase_exports, {
      PluginBase: () => PluginBase2
    });
    module2.exports = __toCommonJS2(PluginBase_exports);
    var import_obsidian5 = require("obsidian");
    var import_Error2 = require_Error();
    var import_ChainedPromise3 = require_ChainedPromise();
    var import_PluginSettings = require_PluginSettings();
    var __import_meta_url9 = globalThis["import.meta.url"] ?? (() => {
      if (typeof __filename !== "string") {
        return new URL(window.location.href);
      }
      return require("node:url").pathToFileURL(__filename);
    })();
    var __process9 = globalThis["process"] ?? {
      "cwd": () => "/",
      "env": {},
      "platform": "android"
    };
    var PluginBase2 = class extends import_obsidian5.Plugin {
      _settings;
      notice;
      _abortSignal;
      /**
       * Gets the AbortSignal used for aborting long-running operations.
       *
       * @returns The abort signal.
       */
      get abortSignal() {
        return this._abortSignal;
      }
      /**
       * Gets a copy of the current plugin settings.
       *
       * @returns A copy of the plugin settings.
       */
      get settingsCopy() {
        return (0, import_PluginSettings.clonePluginSettings)(this.createDefaultPluginSettings.bind(this), this.settings);
      }
      /**
       * Gets the plugin settings.
       *
       * @returns The plugin settings.
       */
      get settings() {
        return this._settings;
      }
      /**
       * Called when the plugin is loaded
       */
      onload() {
        this.register((0, import_Error2.registerAsyncErrorEventHandler)(() => {
          this.showNotice("An unhandled error occurred. Please check the console for more information.");
        }));
        (0, import_ChainedPromise3.chainAsyncFn)(this.app, async () => {
          await this.loadSettings();
          const pluginSettingsTab = this.createPluginSettingsTab();
          if (pluginSettingsTab) {
            this.addSettingTab(pluginSettingsTab);
          }
          const abortController = new AbortController();
          this._abortSignal = abortController.signal;
          this.register(() => {
            abortController.abort();
          });
          await this.onloadComplete();
          this.app.workspace.onLayoutReady(this.onLayoutReady.bind(this));
        });
      }
      /**
       * Called when the layout is ready. This method can be overridden by subclasses to perform actions once
       * the layout is ready.
       *
       * @returns A promise or void indicating the completion of the layout setup.
       */
      onLayoutReady() {
      }
      /**
       * Loads the plugin settings from the saved data.
       *
       * @returns A promise that resolves when the settings are loaded.
       */
      async loadSettings() {
        const data = await this.loadData();
        this._settings = await this.parseSettings(data);
      }
      /**
       * Parses the provided settings data and returns the parsed `PluginSettings`.
       *
       * @param data - The raw data to be parsed into `PluginSettings`.
       * @returns A promise that resolves to `PluginSettings` or the settings directly.
       */
      parseSettings(data) {
        return (0, import_PluginSettings.loadPluginSettings)(this.createDefaultPluginSettings.bind(this), data);
      }
      /**
       * Saves the new plugin settings.
       *
       * @param newSettings - The new settings to save.
       * @returns A promise that resolves when the settings are saved.
       */
      async saveSettings(newSettings) {
        this._settings = (0, import_PluginSettings.clonePluginSettings)(this.createDefaultPluginSettings.bind(this), newSettings);
        await this.saveData(this.settings);
      }
      /**
       * Displays a notice message to the user.
       *
       * @param message - The message to display.
       */
      showNotice(message) {
        if (this.notice) {
          this.notice.hide();
        }
        this.notice = new import_obsidian5.Notice(`${this.manifest.name}
${message}`);
      }
    };
  }
});

// node_modules/obsidian-dev-utils/dist/lib/obsidian/AttachmentPath.cjs
var require_AttachmentPath = __commonJS({
  "node_modules/obsidian-dev-utils/dist/lib/obsidian/AttachmentPath.cjs"(exports2, module2) {
    (function patchRequireEsmDefault() {
      const __require = require;
      require = Object.assign((id) => {
        const module3 = __require(id);
        return module3.__esModule && module3.default ? module3.default : module3;
      }, __require);
    })();
    var __defProp2 = Object.defineProperty;
    var __getOwnPropDesc2 = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames2 = Object.getOwnPropertyNames;
    var __hasOwnProp2 = Object.prototype.hasOwnProperty;
    var __export2 = (target, all) => {
      for (var name in all)
        __defProp2(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps2 = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames2(from))
          if (!__hasOwnProp2.call(to, key) && key !== except)
            __defProp2(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc2(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps2(__defProp2({}, "__esModule", { value: true }), mod);
    var AttachmentPath_exports = {};
    __export2(AttachmentPath_exports, {
      getAttachmentFilePath: () => getAttachmentFilePath,
      getAttachmentFolderPath: () => getAttachmentFolderPath,
      getAvailablePathForAttachments: () => getAvailablePathForAttachments
    });
    module2.exports = __toCommonJS2(AttachmentPath_exports);
    var import_implementations = require_implementations();
    var import_Path = require_Path();
    var import_String = require_String();
    var import_FileSystem3 = require_FileSystem();
    var __import_meta_url9 = globalThis["import.meta.url"] ?? (() => {
      if (typeof __filename !== "string") {
        return new URL(window.location.href);
      }
      return require("node:url").pathToFileURL(__filename);
    })();
    var __process9 = globalThis["process"] ?? {
      "cwd": () => "/",
      "env": {},
      "platform": "android"
    };
    async function getAttachmentFolderPath(app, notePathOrFile) {
      return (0, import_implementations.parentFolderPath)(await getAttachmentFilePath(app, "DUMMY_FILE.pdf", notePathOrFile));
    }
    async function getAttachmentFilePath(app, attachmentPathOrFile, notePathOrFile) {
      const attachmentPath = (0, import_FileSystem3.getPath)(attachmentPathOrFile);
      const notePath = (0, import_FileSystem3.getPath)(notePathOrFile);
      const note = (0, import_FileSystem3.getFile)(app, notePath, true);
      const ext = (0, import_Path.extname)(attachmentPath);
      const fileName = (0, import_Path.basename)(attachmentPath, ext);
      const internalFn = app.vault.getAvailablePathForAttachments;
      if (internalFn.isExtended) {
        return internalFn(fileName, ext.slice(1), note, true);
      }
      return await getAvailablePathForAttachments(app, fileName, ext.slice(1), note, true);
    }
    async function getAvailablePathForAttachments(app, filename, extension, file, skipFolderCreation) {
      let attachmentFolderPath = app.vault.getConfig("attachmentFolderPath");
      const isCurrentFolder = attachmentFolderPath === "." || attachmentFolderPath === "./";
      let relativePath = null;
      if (attachmentFolderPath.startsWith("./")) {
        relativePath = (0, import_String.trimStart)(attachmentFolderPath, "./");
      }
      if (isCurrentFolder) {
        attachmentFolderPath = file ? file.parent?.path ?? "" : "";
      } else if (relativePath) {
        attachmentFolderPath = (file ? file.parent?.getParentPrefix() ?? "" : "") + relativePath;
      }
      attachmentFolderPath = (0, import_String.normalize)(normalizeSlashes(attachmentFolderPath));
      filename = (0, import_String.normalize)(normalizeSlashes(filename));
      let folder = (0, import_FileSystem3.getFolderOrNull)(app, attachmentFolderPath, true);
      if (!folder && relativePath) {
        if (!skipFolderCreation) {
          folder = await app.vault.createFolder(attachmentFolderPath);
        } else {
          folder = (0, import_FileSystem3.getFolder)(app, attachmentFolderPath, true);
        }
      }
      const prefix = folder?.getParentPrefix() ?? "";
      return app.vault.getAvailablePath(prefix + filename, extension);
    }
    function normalizeSlashes(path) {
      path = path.replace(/([\\/])+/g, "/");
      path = path.replace(/(^\/+|\/+$)/g, "");
      return path || "/";
    }
  }
});

// node_modules/obsidian-dev-utils/dist/lib/obsidian/RenameDeleteHandler.cjs
var require_RenameDeleteHandler = __commonJS({
  "node_modules/obsidian-dev-utils/dist/lib/obsidian/RenameDeleteHandler.cjs"(exports2, module2) {
    (function patchRequireEsmDefault() {
      const __require = require;
      require = Object.assign((id) => {
        const module3 = __require(id);
        return module3.__esModule && module3.default ? module3.default : module3;
      }, __require);
    })();
    var __defProp2 = Object.defineProperty;
    var __getOwnPropDesc2 = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames2 = Object.getOwnPropertyNames;
    var __hasOwnProp2 = Object.prototype.hasOwnProperty;
    var __export2 = (target, all) => {
      for (var name in all)
        __defProp2(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps2 = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames2(from))
          if (!__hasOwnProp2.call(to, key) && key !== except)
            __defProp2(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc2(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps2(__defProp2({}, "__esModule", { value: true }), mod);
    var RenameDeleteHandler_exports = {};
    __export2(RenameDeleteHandler_exports, {
      registerRenameDeleteHandlers: () => registerRenameDeleteHandlers2
    });
    module2.exports = __toCommonJS2(RenameDeleteHandler_exports);
    var import_obsidian5 = require("obsidian");
    var import_Object = require_Object();
    var import_Path = require_Path();
    var import_App = require_App();
    var import_AttachmentPath = require_AttachmentPath();
    var import_ChainedPromise3 = require_ChainedPromise();
    var import_FileSystem3 = require_FileSystem();
    var import_Link4 = require_Link();
    var import_MetadataCache2 = require_MetadataCache();
    var import_Vault2 = require_Vault();
    var __import_meta_url9 = globalThis["import.meta.url"] ?? (() => {
      if (typeof __filename !== "string") {
        return new URL(window.location.href);
      }
      return require("node:url").pathToFileURL(__filename);
    })();
    var __process9 = globalThis["process"] ?? {
      "cwd": () => "/",
      "env": {},
      "platform": "android"
    };
    var specialRenames = [];
    var deletedMetadataCacheMap = /* @__PURE__ */ new Map();
    function registerRenameDeleteHandlers2(plugin, settingsBuilder) {
      const renameDeleteHandlersMap = getRenameDeleteHandlersMap(plugin.app);
      const pluginId = plugin.manifest.id;
      renameDeleteHandlersMap.set(pluginId, settingsBuilder);
      logPluginSettingsOrder(plugin.app);
      plugin.register(() => {
        renameDeleteHandlersMap.delete(pluginId);
        logPluginSettingsOrder(plugin.app);
      });
      const app = plugin.app;
      plugin.registerEvent(
        app.vault.on("delete", (file) => {
          if (!shouldInvokeHandler(app, pluginId, "Delete")) {
            return;
          }
          (0, import_ChainedPromise3.chainAsyncFn)(app, () => handleDelete(app, file));
        })
      );
      plugin.registerEvent(
        app.vault.on("rename", (file, oldPath) => {
          if (!shouldInvokeHandler(app, pluginId, "Rename")) {
            return;
          }
          (0, import_ChainedPromise3.chainAsyncFn)(app, () => handleRename(app, file, oldPath));
        })
      );
      plugin.registerEvent(
        app.metadataCache.on("deleted", (file, prevCache) => {
          handleMetadataDeleted(file, prevCache);
        })
      );
    }
    function shouldInvokeHandler(app, pluginId, handlerType) {
      const renameDeleteHandlerPluginIds = getRenameDeleteHandlersMap(app);
      const mainPluginId = Array.from(renameDeleteHandlerPluginIds.keys())[0];
      if (mainPluginId !== pluginId) {
        console.debug(`${handlerType} handler for plugin ${pluginId} is skipped, because it is handled by plugin ${mainPluginId ?? "(none)"}`);
        return false;
      }
      return true;
    }
    function getRenameDeleteHandlersMap(app) {
      return (0, import_App.getObsidianDevUtilsState)(app, "renameDeleteHandlersMap", /* @__PURE__ */ new Map()).value;
    }
    function logPluginSettingsOrder(app) {
      const renameDeleteHandlersMap = getRenameDeleteHandlersMap(app);
      console.debug(`Rename/delete handlers will use plugin settings in the following order: ${Array.from(renameDeleteHandlersMap.keys()).join(", ")}`);
    }
    async function handleRename(app, file, oldPath) {
      console.debug(`Handle Rename ${oldPath} -> ${file.path}`);
      if (!(file instanceof import_obsidian5.TFile)) {
        return;
      }
      const specialRename = specialRenames.find((x) => x.oldPath === file.path);
      if (specialRename) {
        const newTempPath = await (0, import_Vault2.renameSafe)(app, file, specialRename.tempPath);
        specialRename.tempPath = newTempPath;
        return;
      }
      if (app.vault.adapter.insensitive && oldPath.toLowerCase() === file.path.toLowerCase() && (0, import_Path.dirname)(oldPath) === (0, import_Path.dirname)(file.path)) {
        specialRenames.push({
          oldPath,
          newPath: file.path,
          tempPath: (0, import_Path.join)(file.parent?.path ?? "", "__temp__" + file.name)
        });
        await (0, import_Vault2.renameSafe)(app, file, oldPath);
        return;
      }
      const updateAllLinks = app.fileManager.updateAllLinks;
      try {
        app.fileManager.updateAllLinks = async () => {
        };
        const renameMap = /* @__PURE__ */ new Map();
        await fillRenameMap(app, file, oldPath, renameMap);
        renameMap.set(oldPath, file.path);
        for (const [oldPath2, newPath2] of renameMap.entries()) {
          await processRename(app, oldPath2, newPath2, renameMap);
        }
      } finally {
        app.fileManager.updateAllLinks = updateAllLinks;
        const specialRename2 = specialRenames.find((x) => x.tempPath === file.path);
        if (specialRename2) {
          await (0, import_Vault2.renameSafe)(app, file, specialRename2.newPath);
          specialRenames.remove(specialRename2);
        }
      }
    }
    async function handleDelete(app, file) {
      console.debug(`Handle Delete ${file.path}`);
      if (!(0, import_FileSystem3.isNote)(file)) {
        return;
      }
      const settings = getSettings(app);
      if (!settings.shouldDeleteOrphanAttachments) {
        return;
      }
      const cache = deletedMetadataCacheMap.get(file.path);
      deletedMetadataCacheMap.delete(file.path);
      if (cache) {
        const links = (0, import_MetadataCache2.getAllLinks)(cache);
        for (const link of links) {
          const attachmentFile = (0, import_Link4.extractLinkFile)(app, link, file.path);
          if (!attachmentFile) {
            continue;
          }
          if ((0, import_FileSystem3.isNote)(attachmentFile)) {
            continue;
          }
          await (0, import_Vault2.deleteSafe)(app, attachmentFile, file.path, settings.shouldDeleteEmptyFolders);
        }
      }
      const attachmentFolderPath = await (0, import_AttachmentPath.getAttachmentFolderPath)(app, file.path);
      const attachmentFolder = (0, import_FileSystem3.getFolderOrNull)(app, attachmentFolderPath);
      if (!attachmentFolder) {
        return;
      }
      await (0, import_Vault2.deleteSafe)(app, attachmentFolder, file.path, false, settings.shouldDeleteEmptyFolders);
    }
    async function fillRenameMap(app, file, oldPath, renameMap) {
      if (!(0, import_FileSystem3.isNote)(file)) {
        return;
      }
      const settings = getSettings(app);
      const oldAttachmentFolderPath = await (0, import_AttachmentPath.getAttachmentFolderPath)(app, oldPath);
      const newAttachmentFolderPath = settings.shouldRenameAttachmentFolder ? await (0, import_AttachmentPath.getAttachmentFolderPath)(app, file.path) : oldAttachmentFolderPath;
      const dummyOldAttachmentFolderPath = await (0, import_AttachmentPath.getAttachmentFolderPath)(app, (0, import_Path.join)((0, import_Path.dirname)(oldPath), "DUMMY_FILE.md"));
      const oldAttachmentFolder = (0, import_FileSystem3.getFolderOrNull)(app, oldAttachmentFolderPath);
      if (!oldAttachmentFolder) {
        return;
      }
      if (oldAttachmentFolderPath === newAttachmentFolderPath && !settings.shouldRenameAttachmentFiles) {
        return;
      }
      const children = [];
      if (oldAttachmentFolderPath === dummyOldAttachmentFolderPath) {
        const cache = await (0, import_MetadataCache2.getCacheSafe)(app, file);
        if (!cache) {
          return;
        }
        for (const link of (0, import_MetadataCache2.getAllLinks)(cache)) {
          const attachmentFile = (0, import_Link4.extractLinkFile)(app, link, oldPath);
          if (!attachmentFile) {
            continue;
          }
          if (attachmentFile.path.startsWith(oldAttachmentFolderPath)) {
            const backlinks = await (0, import_MetadataCache2.getBacklinksForFileSafe)(app, attachmentFile);
            if (backlinks.keys().length === 1) {
              children.push(attachmentFile);
            }
          }
        }
      } else {
        import_obsidian5.Vault.recurseChildren(oldAttachmentFolder, (child) => {
          if (child instanceof import_obsidian5.TFile) {
            children.push(child);
          }
        });
      }
      const oldNoteBaseName = (0, import_Path.basename)(oldPath, (0, import_Path.extname)(oldPath));
      for (const child of children) {
        if ((0, import_FileSystem3.isNote)(child)) {
          continue;
        }
        const relativePath = (0, import_Path.relative)(oldAttachmentFolderPath, child.path);
        const newDir = (0, import_Path.join)(newAttachmentFolderPath, (0, import_Path.dirname)(relativePath));
        const newChildBasename = settings.shouldRenameAttachmentFiles ? child.basename.replaceAll(oldNoteBaseName, file.basename) : child.basename;
        let newChildPath = (0, import_Path.join)(newDir, (0, import_Path.makeFileName)(newChildBasename, child.extension));
        if (child.path !== newChildPath) {
          if (settings.shouldDeleteConflictingAttachments) {
            const newChildFile = (0, import_FileSystem3.getFileOrNull)(app, newChildPath);
            if (newChildFile) {
              await app.fileManager.trashFile(newChildFile);
            }
          } else {
            newChildPath = app.vault.getAvailablePath((0, import_Path.join)(newDir, newChildBasename), child.extension);
          }
          renameMap.set(child.path, newChildPath);
        }
      }
    }
    async function processRename(app, oldPath, newPath, renameMap) {
      const settings = getSettings(app);
      let oldFile = (0, import_FileSystem3.getFileOrNull)(app, oldPath);
      if (oldFile) {
        const oldFolder = oldFile.parent;
        newPath = await (0, import_Vault2.renameSafe)(app, oldFile, newPath);
        renameMap.set(oldPath, newPath);
        if (settings.shouldDeleteEmptyFolders) {
          await (0, import_Vault2.deleteEmptyFolderHierarchy)(app, oldFolder);
        }
      }
      oldFile = (0, import_FileSystem3.getFile)(app, oldPath, true);
      const newFile = (0, import_FileSystem3.getFileOrNull)(app, newPath);
      if (!oldFile.deleted || !newFile) {
        throw new Error(`Could not rename ${oldPath} to ${newPath}`);
      }
      if (!settings.shouldUpdateLinks) {
        return;
      }
      const backlinks = await (0, import_MetadataCache2.getBacklinksMap)(app, [oldFile, newFile]);
      for (const parentNotePath of backlinks.keys()) {
        let parentNote = (0, import_FileSystem3.getFileOrNull)(app, parentNotePath);
        if (!parentNote) {
          const newParentNotePath = renameMap.get(parentNotePath);
          if (newParentNotePath) {
            parentNote = (0, import_FileSystem3.getFileOrNull)(app, newParentNotePath);
          }
        }
        if (!parentNote) {
          console.warn(`Parent note not found: ${parentNotePath}`);
          continue;
        }
        await (0, import_Vault2.applyFileChanges)(app, parentNote, async () => {
          const backlinks2 = await (0, import_MetadataCache2.getBacklinksMap)(app, [oldFile, newFile]);
          const links = backlinks2.get(parentNotePath) ?? [];
          const changes = [];
          for (const link of links) {
            changes.push({
              startIndex: link.position.start.offset,
              endIndex: link.position.end.offset,
              oldContent: link.original,
              newContent: (0, import_Link4.updateLink)({
                app,
                link,
                pathOrFile: newFile,
                oldPathOrFile: oldPath,
                sourcePathOrFile: parentNote,
                renameMap,
                shouldUpdateFilenameAlias: settings.shouldUpdateFilenameAliases
              })
            });
          }
          return changes;
        });
      }
      if ((0, import_FileSystem3.isCanvasFile)(newFile)) {
        await (0, import_Vault2.processWithRetry)(app, newFile, (content) => {
          const canvasData = JSON.parse(content);
          for (const node of canvasData.nodes) {
            if (node.type !== "file") {
              continue;
            }
            const newPath2 = renameMap.get(node.file);
            if (!newPath2) {
              continue;
            }
            node.file = newPath2;
          }
          return (0, import_Object.toJson)(canvasData);
        });
      } else if ((0, import_FileSystem3.isMarkdownFile)(newFile)) {
        await (0, import_Link4.updateLinksInFile)({
          app,
          pathOrFile: newFile,
          oldPathOrFile: oldPath,
          renameMap,
          shouldUpdateFilenameAlias: settings.shouldUpdateFilenameAliases
        });
      }
    }
    function getSettings(app) {
      const renameDeleteHandlersMap = getRenameDeleteHandlersMap(app);
      const settingsBuilders = Array.from(renameDeleteHandlersMap.values()).reverse();
      const settings = {};
      for (const settingsBuilder of settingsBuilders) {
        const newSettings = settingsBuilder();
        for (const [key, value] of Object.entries(newSettings)) {
          settings[key] ||= value;
        }
      }
      return settings;
    }
    function handleMetadataDeleted(file, prevCache) {
      if ((0, import_FileSystem3.isMarkdownFile)(file) && prevCache) {
        deletedMetadataCacheMap.set(file.path, prevCache);
      }
    }
  }
});

// node_modules/obsidian-dev-utils/dist/lib/DocumentFragment.cjs
var require_DocumentFragment = __commonJS({
  "node_modules/obsidian-dev-utils/dist/lib/DocumentFragment.cjs"(exports2, module2) {
    (function patchRequireEsmDefault() {
      const __require = require;
      require = Object.assign((id) => {
        const module3 = __require(id);
        return module3.__esModule && module3.default ? module3.default : module3;
      }, __require);
    })();
    var __defProp2 = Object.defineProperty;
    var __getOwnPropDesc2 = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames2 = Object.getOwnPropertyNames;
    var __hasOwnProp2 = Object.prototype.hasOwnProperty;
    var __export2 = (target, all) => {
      for (var name in all)
        __defProp2(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps2 = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames2(from))
          if (!__hasOwnProp2.call(to, key) && key !== except)
            __defProp2(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc2(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps2(__defProp2({}, "__esModule", { value: true }), mod);
    var DocumentFragment_exports = {};
    __export2(DocumentFragment_exports, {
      appendCodeBlock: () => appendCodeBlock2
    });
    module2.exports = __toCommonJS2(DocumentFragment_exports);
    var __import_meta_url9 = globalThis["import.meta.url"] ?? (() => {
      if (typeof __filename !== "string") {
        return new URL(window.location.href);
      }
      return require("node:url").pathToFileURL(__filename);
    })();
    var __process9 = globalThis["process"] ?? {
      "cwd": () => "/",
      "env": {},
      "platform": "android"
    };
    function appendCodeBlock2(fragment, code) {
      fragment.appendChild(createSpan({ cls: "markdown-rendered code" }, (span) => {
        span.style.fontWeight = "bold";
        span.appendChild(createEl("code", { text: code }));
      }));
    }
  }
});

// node_modules/obsidian-dev-utils/dist/lib/obsidian/Plugin/PluginSettingsTabBase.cjs
var require_PluginSettingsTabBase = __commonJS({
  "node_modules/obsidian-dev-utils/dist/lib/obsidian/Plugin/PluginSettingsTabBase.cjs"(exports2, module2) {
    (function patchRequireEsmDefault() {
      const __require = require;
      require = Object.assign((id) => {
        const module3 = __require(id);
        return module3.__esModule && module3.default ? module3.default : module3;
      }, __require);
    })();
    var __defProp2 = Object.defineProperty;
    var __getOwnPropDesc2 = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames2 = Object.getOwnPropertyNames;
    var __hasOwnProp2 = Object.prototype.hasOwnProperty;
    var __export2 = (target, all) => {
      for (var name in all)
        __defProp2(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps2 = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames2(from))
          if (!__hasOwnProp2.call(to, key) && key !== except)
            __defProp2(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc2(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps2(__defProp2({}, "__esModule", { value: true }), mod);
    var PluginSettingsTabBase_exports = {};
    __export2(PluginSettingsTabBase_exports, {
      PluginSettingsTabBase: () => PluginSettingsTabBase2
    });
    module2.exports = __toCommonJS2(PluginSettingsTabBase_exports);
    var import_obsidian5 = require("obsidian");
    var import_PluginBase2 = require_PluginBase();
    var __import_meta_url9 = globalThis["import.meta.url"] ?? (() => {
      if (typeof __filename !== "string") {
        return new URL(window.location.href);
      }
      return require("node:url").pathToFileURL(__filename);
    })();
    var __process9 = globalThis["process"] ?? {
      "cwd": () => "/",
      "env": {},
      "platform": "android"
    };
    var PluginSettingsTabBase2 = class extends import_obsidian5.PluginSettingTab {
      constructor(plugin) {
        super(plugin.app, plugin);
        this.plugin = plugin;
      }
    };
  }
});

// node_modules/obsidian-dev-utils/dist/lib/obsidian/Plugin/UIComponent.cjs
var require_UIComponent = __commonJS({
  "node_modules/obsidian-dev-utils/dist/lib/obsidian/Plugin/UIComponent.cjs"(exports2, module2) {
    (function patchRequireEsmDefault() {
      const __require = require;
      require = Object.assign((id) => {
        const module3 = __require(id);
        return module3.__esModule && module3.default ? module3.default : module3;
      }, __require);
    })();
    var __defProp2 = Object.defineProperty;
    var __getOwnPropDesc2 = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames2 = Object.getOwnPropertyNames;
    var __hasOwnProp2 = Object.prototype.hasOwnProperty;
    var __export2 = (target, all) => {
      for (var name in all)
        __defProp2(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps2 = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames2(from))
          if (!__hasOwnProp2.call(to, key) && key !== except)
            __defProp2(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc2(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS2 = (mod) => __copyProps2(__defProp2({}, "__esModule", { value: true }), mod);
    var UIComponent_exports = {};
    __export2(UIComponent_exports, {
      bindUiComponent: () => bindUiComponent2
    });
    module2.exports = __toCommonJS2(UIComponent_exports);
    var import_obsidian5 = require("obsidian");
    var __import_meta_url9 = globalThis["import.meta.url"] ?? (() => {
      if (typeof __filename !== "string") {
        return new URL(window.location.href);
      }
      return require("node:url").pathToFileURL(__filename);
    })();
    var __process9 = globalThis["process"] ?? {
      "cwd": () => "/",
      "env": {},
      "platform": "android"
    };
    function bindUiComponent2(plugin, uiComponent, property, options) {
      const DEFAULT_OPTIONS = {
        autoSave: true,
        settingToUIValueConverter: (value) => value,
        uiToSettingValueConverter: (value) => value
      };
      const optionsExt = { ...DEFAULT_OPTIONS, ...options };
      const pluginExt = plugin;
      const uiComponentExt = uiComponent;
      const pluginSettingsFn = () => optionsExt.pluginSettings ?? pluginExt.settingsCopy;
      uiComponentExt.setValue(optionsExt.settingToUIValueConverter(pluginSettingsFn()[property])).onChange(async (uiValue) => {
        if (optionsExt.uiValueValidator) {
          const errorMessage = optionsExt.uiValueValidator(uiValue);
          const validatorElement = getValidatorElement(uiComponent);
          if (validatorElement) {
            validatorElement.setCustomValidity(errorMessage ?? "");
            validatorElement.reportValidity();
          }
          if (errorMessage) {
            return;
          }
        }
        const pluginSettings = pluginSettingsFn();
        pluginSettings[property] = optionsExt.uiToSettingValueConverter(uiValue);
        if (optionsExt.autoSave) {
          await pluginExt.saveSettings(pluginSettings);
        }
      });
      return uiComponent;
    }
    function getValidatorElement(uiComponent) {
      if (uiComponent instanceof import_obsidian5.DropdownComponent) {
        return uiComponent.selectEl;
      }
      if (uiComponent instanceof import_obsidian5.SliderComponent) {
        return uiComponent.sliderEl;
      }
      if (uiComponent instanceof import_obsidian5.TextAreaComponent) {
        return uiComponent.inputEl;
      }
      if (uiComponent instanceof import_obsidian5.TextComponent) {
        return uiComponent.inputEl;
      }
      return null;
    }
  }
});

// src/main.ts
var main_exports = {};
__export(main_exports, {
  default: () => BetterMarkdownLinksPlugin
});
module.exports = __toCommonJS(main_exports);

// node_modules/monkey-around/dist/index.mjs
var __import_meta_url = globalThis["import.meta.url"] ?? (() => {
  if (typeof __filename !== "string") {
    return new URL(window.location.href);
  }
  return require("node:url").pathToFileURL(__filename);
})();
var __process = globalThis["process"] ?? {
  "cwd": () => "/",
  "env": {},
  "platform": "android"
};
function around(obj, factories) {
  const removers = Object.keys(factories).map((key) => around1(obj, key, factories[key]));
  return removers.length === 1 ? removers[0] : function() {
    removers.forEach((r) => r());
  };
}
function around1(obj, method, createWrapper) {
  const inherited = obj[method], hadOwn = obj.hasOwnProperty(method), original = hadOwn ? inherited : function() {
    return Object.getPrototypeOf(obj)[method].apply(this, arguments);
  };
  let current = createWrapper(original);
  if (inherited)
    Object.setPrototypeOf(current, inherited);
  Object.setPrototypeOf(wrapper, current);
  obj[method] = wrapper;
  return remove;
  function wrapper(...args) {
    if (current === original && obj[method] === wrapper)
      remove();
    return current.apply(this, args);
  }
  function remove() {
    if (obj[method] === wrapper) {
      if (hadOwn)
        obj[method] = original;
      else
        delete obj[method];
    }
    if (current === original)
      return;
    current = original;
    Object.setPrototypeOf(wrapper, inherited || Function);
  }
}

// src/BetterMarkdownLinksPlugin.ts
var import_obsidian4 = require("obsidian");
var import_ChainedPromise2 = __toESM(require_ChainedPromise(), 1);
var import_FileSystem2 = __toESM(require_FileSystem(), 1);
var import_Link3 = __toESM(require_Link(), 1);
var import_MetadataCache = __toESM(require_MetadataCache(), 1);
var import_PluginBase = __toESM(require_PluginBase(), 1);
var import_RenameDeleteHandler = __toESM(require_RenameDeleteHandler(), 1);

// src/BetterMarkdownLinksPluginSettings.ts
var __import_meta_url2 = globalThis["import.meta.url"] ?? (() => {
  if (typeof __filename !== "string") {
    return new URL(window.location.href);
  }
  return require("node:url").pathToFileURL(__filename);
})();
var __process2 = globalThis["process"] ?? {
  "cwd": () => "/",
  "env": {},
  "platform": "android"
};
var BetterMarkdownLinksPluginSettings = class {
  allowEmptyEmbedAlias = true;
  automaticallyConvertNewLinks = true;
  automaticallyUpdateLinksOnRenameOrMove = true;
  ignoreIncompatibleObsidianSettings = false;
  includeAttachmentExtensionToEmbedAlias = false;
  useAngleBrackets = true;
  useLeadingDot = true;
};

// src/BetterMarkdownLinksPluginSettingsTab.ts
var import_obsidian = require("obsidian");
var import_DocumentFragment = __toESM(require_DocumentFragment(), 1);
var import_PluginSettingsTabBase = __toESM(require_PluginSettingsTabBase(), 1);
var import_UIComponent = __toESM(require_UIComponent(), 1);
var __import_meta_url3 = globalThis["import.meta.url"] ?? (() => {
  if (typeof __filename !== "string") {
    return new URL(window.location.href);
  }
  return require("node:url").pathToFileURL(__filename);
})();
var __process3 = globalThis["process"] ?? {
  "cwd": () => "/",
  "env": {},
  "platform": "android"
};
var BetterMarkdownLinksPluginSettingsTab = class extends import_PluginSettingsTabBase.PluginSettingsTabBase {
  display() {
    this.containerEl.empty();
    new import_obsidian.Setting(this.containerEl).setName("Use leading dot").setDesc("Use a leading dot in relative links").addToggle((toggle) => (0, import_UIComponent.bindUiComponent)(this.plugin, toggle, "useLeadingDot"));
    new import_obsidian.Setting(this.containerEl).setName("Use angle brackets").setDesc("Use angle brackets in links").addToggle((toggle) => (0, import_UIComponent.bindUiComponent)(this.plugin, toggle, "useAngleBrackets"));
    new import_obsidian.Setting(this.containerEl).setName("Automatically convert new links").setDesc("Automatically convert new links entered manually to the selected format").addToggle((toggle) => (0, import_UIComponent.bindUiComponent)(this.plugin, toggle, "automaticallyConvertNewLinks"));
    new import_obsidian.Setting(this.containerEl).setName("Automatically update links on rename or move").setDesc(createFragment((f) => {
      f.appendText("Automatically update links when a file is renamed or moved to another directory.");
      f.createEl("br");
      f.appendText("Consider installing the ");
      f.createEl("a", {
        href: "obsidian://show-plugin?id=backlink-cache",
        text: "Backlink Cache"
      });
      f.appendText(" plugin to improve performance.");
    })).addToggle((toggle) => (0, import_UIComponent.bindUiComponent)(this.plugin, toggle, "automaticallyUpdateLinksOnRenameOrMove"));
    new import_obsidian.Setting(this.containerEl).setName("Ignore incompatible Obsidian settings").setDesc(createFragment((f) => {
      f.appendText("Current plugin makes sense only if you have ");
      (0, import_DocumentFragment.appendCodeBlock)(f, "Use [[Wikilinks]]");
      f.appendText(" disabled and ");
      (0, import_DocumentFragment.appendCodeBlock)(f, "New link format");
      f.appendText(" set to ");
      (0, import_DocumentFragment.appendCodeBlock)(f, "Relative path to file");
      f.appendText(" in Obsidian settings.");
      f.createEl("br");
      f.appendText("If you enable current setting, it will override incompatible Obsidian settings and will work as expected.");
    })).addToggle((toggle) => (0, import_UIComponent.bindUiComponent)(this.plugin, toggle, "ignoreIncompatibleObsidianSettings"));
    new import_obsidian.Setting(this.containerEl).setName("Allow empty embed alias").setDesc("If disabled, empty alias will be replaced with the attachment name").addToggle((toggle) => (0, import_UIComponent.bindUiComponent)(this.plugin, toggle, "allowEmptyEmbedAlias"));
    new import_obsidian.Setting(this.containerEl).setName("Include attachment extension to embed alias").setDesc(createFragment((f) => {
      f.appendText("If enabled, the extension of the attachment will be included in the embed alias.");
      f.createEl("br");
      f.appendText("The setting is ignored if ");
      (0, import_DocumentFragment.appendCodeBlock)(f, "Allow empty embed alias");
      f.appendText(" is enabled.");
    })).addToggle((toggle) => (0, import_UIComponent.bindUiComponent)(this.plugin, toggle, "includeAttachmentExtensionToEmbedAlias"));
  }
};

// src/GenerateMarkdownLink.ts
var import_obsidian2 = require("obsidian");
var import_Link = __toESM(require_Link(), 1);
var __import_meta_url4 = globalThis["import.meta.url"] ?? (() => {
  if (typeof __filename !== "string") {
    return new URL(window.location.href);
  }
  return require("node:url").pathToFileURL(__filename);
})();
var __process4 = globalThis["process"] ?? {
  "cwd": () => "/",
  "env": {},
  "platform": "android"
};
function getDefaultOptions(plugin) {
  const pluginSettings = plugin.settingsCopy;
  return {
    isWikilink: pluginSettings.ignoreIncompatibleObsidianSettings ? false : void 0,
    forceRelativePath: pluginSettings.ignoreIncompatibleObsidianSettings ? true : void 0,
    useLeadingDot: pluginSettings.useLeadingDot,
    useAngleBrackets: pluginSettings.useAngleBrackets,
    allowEmptyEmbedAlias: pluginSettings.allowEmptyEmbedAlias,
    includeAttachmentExtensionToEmbedAlias: pluginSettings.includeAttachmentExtensionToEmbedAlias
  };
}
function generateMarkdownLinkForPlugin(plugin, fileOrOptions, sourcePath, subpath, alias) {
  let options;
  if (fileOrOptions instanceof import_obsidian2.TFile) {
    options = {
      pathOrFile: fileOrOptions,
      sourcePathOrFile: sourcePath,
      subpath,
      alias
    };
  } else {
    options = fileOrOptions;
  }
  return (0, import_Link.generateMarkdownLink)({ app: plugin.app, ...options });
}
function getPatchedGenerateMarkdownLink(plugin) {
  const generateMarkdownLinkFn = (fileOrOptions, sourcePath, subpath, alias) => generateMarkdownLinkForPlugin(plugin, fileOrOptions, sourcePath, subpath, alias);
  const generateMarkdownLinkDefaultOptionsWrapper = {
    defaultOptionsFn: () => getDefaultOptions(plugin)
  };
  return Object.assign(generateMarkdownLinkFn, generateMarkdownLinkDefaultOptionsWrapper);
}

// src/LinkConverter.ts
var import_obsidian3 = require("obsidian");
var import_Error = __toESM(require_Error(), 1);
var import_ChainedPromise = __toESM(require_ChainedPromise(), 1);
var import_FileSystem = __toESM(require_FileSystem(), 1);
var import_Link2 = __toESM(require_Link(), 1);
var import_Vault = __toESM(require_Vault(), 1);

// src/ObsidianSettings.ts
var import_ObsidianSettings = __toESM(require_ObsidianSettings(), 1);
var __import_meta_url5 = globalThis["import.meta.url"] ?? (() => {
  if (typeof __filename !== "string") {
    return new URL(window.location.href);
  }
  return require("node:url").pathToFileURL(__filename);
})();
var __process5 = globalThis["process"] ?? {
  "cwd": () => "/",
  "env": {},
  "platform": "android"
};
function checkObsidianSettingsCompatibility(plugin) {
  const app = plugin.app;
  if (plugin.settingsCopy.ignoreIncompatibleObsidianSettings) {
    return true;
  }
  if (!(0, import_ObsidianSettings.shouldUseWikilinks)(app) && (0, import_ObsidianSettings.shouldUseRelativeLinks)(app)) {
    return true;
  }
  plugin.showCompatibilityWarning();
  return false;
}

// src/LinkConverter.ts
var __import_meta_url6 = globalThis["import.meta.url"] ?? (() => {
  if (typeof __filename !== "string") {
    return new URL(window.location.href);
  }
  return require("node:url").pathToFileURL(__filename);
})();
var __process6 = globalThis["process"] ?? {
  "cwd": () => "/",
  "env": {},
  "platform": "android"
};
function convertLinksInCurrentFile(plugin, checking) {
  const activeFile = plugin.app.workspace.getActiveFile();
  if (!activeFile || !(0, import_FileSystem.isMarkdownFile)(activeFile)) {
    return false;
  }
  if (!checking) {
    (0, import_ChainedPromise.chainAsyncFn)(plugin.app, () => convertLinksInFile(plugin, activeFile));
  }
  return true;
}
async function convertLinksInFile(plugin, file) {
  if (!checkObsidianSettingsCompatibility(plugin)) {
    return;
  }
  await (0, import_Link2.updateLinksInFile)({
    app: plugin.app,
    pathOrFile: file
  });
}
async function convertLinksInEntireVault(plugin, abortSignal) {
  if (!checkObsidianSettingsCompatibility(plugin)) {
    return;
  }
  const mdFiles = (0, import_Vault.getMarkdownFilesSorted)(plugin.app);
  let index = 0;
  const notice = new import_obsidian3.Notice("", 0);
  for (const file of mdFiles) {
    if (abortSignal.aborted) {
      break;
    }
    index++;
    const message = `Converting links in note # ${index.toString()} / ${mdFiles.length.toString()}: ${file.path}`;
    notice.setMessage(message);
    console.log(message);
    try {
      await convertLinksInFile(plugin, file);
    } catch (e) {
      (0, import_Error.emitAsyncErrorEvent)(e);
    }
  }
  notice.hide();
}
async function applyLinkChangeUpdates(plugin, file, updates) {
  await (0, import_Vault.applyFileChanges)(plugin.app, file, async () => {
    const changes = updates.map((update) => ({
      startIndex: update.reference.position.start.offset,
      endIndex: update.reference.position.end.offset,
      oldContent: update.reference.original,
      newContent: fixChange(plugin, update.change, file)
    }));
    const content = await plugin.app.vault.read(file);
    const doUpdatesMatchContent = changes.every((change) => content.slice(change.startIndex, change.endIndex) === change.oldContent);
    return doUpdatesMatchContent ? changes : [];
  });
}
function fixChange(plugin, change, file) {
  const match = /^!?\[(.*?)\]\(([^<]+?) .+?>\)$/.exec(change);
  const isEmbed = change.startsWith("!");
  if (!match) {
    return change;
  }
  const alias = match[1] ?? "";
  const escapedPath = match[2] ?? "";
  const { linkPath, subpath } = (0, import_Link2.splitSubpath)(decodeURIComponent(escapedPath));
  const linkedFile = plugin.app.metadataCache.getFirstLinkpathDest(linkPath, file.path);
  if (!linkedFile) {
    return `${isEmbed ? "!" : ""}[${alias}](${escapedPath})`;
  }
  return (0, import_Link2.generateMarkdownLink)({
    app: plugin.app,
    pathOrFile: linkedFile,
    sourcePathOrFile: file,
    subpath,
    alias,
    isEmbed,
    isWikilink: false
  });
}

// src/BetterMarkdownLinksPlugin.ts
var __import_meta_url7 = globalThis["import.meta.url"] ?? (() => {
  if (typeof __filename !== "string") {
    return new URL(window.location.href);
  }
  return require("node:url").pathToFileURL(__filename);
})();
var __process7 = globalThis["process"] ?? {
  "cwd": () => "/",
  "env": {},
  "platform": "android"
};
var BetterMarkdownLinksPlugin = class extends import_PluginBase.PluginBase {
  warningNotice;
  createDefaultPluginSettings() {
    return new BetterMarkdownLinksPluginSettings();
  }
  createPluginSettingsTab() {
    return new BetterMarkdownLinksPluginSettingsTab(this);
  }
  onloadComplete() {
    this.register(around(this.app.fileManager, {
      generateMarkdownLink: () => getPatchedGenerateMarkdownLink(this)
    }));
    this.addCommand({
      id: "convert-links-in-current-file",
      name: "Convert links in current file",
      checkCallback: (checking) => convertLinksInCurrentFile(this, checking)
    });
    this.addCommand({
      id: "convert-links-in-entire-vault",
      name: "Convert links in entire vault",
      callback: () => convertLinksInEntireVault(this, this.abortSignal)
    });
    this.registerEvent(this.app.metadataCache.on("changed", (file) => {
      (0, import_ChainedPromise2.chainAsyncFn)(this.app, () => this.handleMetadataCacheChanged(file));
    }));
    (0, import_RenameDeleteHandler.registerRenameDeleteHandlers)(this, () => {
      const settings = {
        shouldUpdateFilenameAliases: true,
        shouldUpdateLinks: this.settings.automaticallyUpdateLinksOnRenameOrMove
      };
      return settings;
    });
    this.warningNotice = new import_obsidian4.Notice("");
    this.warningNotice.hide();
    this.app.fileManager.linkUpdaters[import_FileSystem2.MARKDOWN_FILE_EXTENSION] = {
      applyUpdates: (file, updates) => applyLinkChangeUpdates(this, file, updates),
      iterateReferences: () => {
      },
      renameSubpath: async () => {
      }
    };
    this.register(() => {
      delete this.app.fileManager.linkUpdaters[import_FileSystem2.MARKDOWN_FILE_EXTENSION];
    });
  }
  showCompatibilityWarning() {
    const message = 'Your Obsidian settings are incompatible with the "Better Markdown Links" plugin. Please disable "Use [[Wikilinks]]" and set "New link format" to "Relative path to file" in Obsidian settings.\nAlternatively, you can enable the "Ignore incompatible Obsidian settings" option in the plugin settings.';
    console.warn(message);
    if (this.warningNotice.noticeEl.style.opacity === "0") {
      this.warningNotice = new import_obsidian4.Notice(message, 1e4);
    }
  }
  async handleMetadataCacheChanged(file) {
    if (!this.settings.automaticallyConvertNewLinks) {
      return;
    }
    const suggestionContainer = document.querySelector(".suggestion-container");
    if (suggestionContainer && suggestionContainer.style.display !== "none") {
      return;
    }
    const cache = await (0, import_MetadataCache.getCacheSafe)(this.app, file);
    if (!cache) {
      return;
    }
    const links = (0, import_MetadataCache.getAllLinks)(cache);
    if (links.some((link) => link.original !== (0, import_Link3.convertLink)({
      app: this.app,
      link,
      sourcePathOrFile: file
    }))) {
      await convertLinksInFile(this, file);
    }
  }
};

// src/main.ts
var __import_meta_url8 = globalThis["import.meta.url"] ?? (() => {
  if (typeof __filename !== "string") {
    return new URL(window.location.href);
  }
  return require("node:url").pathToFileURL(__filename);
})();
var __process8 = globalThis["process"] ?? {
  "cwd": () => "/",
  "env": {},
  "platform": "android"
};
